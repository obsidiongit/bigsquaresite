# Build Log

Session history moved out of `tasks.md` on 2026-08-25 so the task list stays scannable. Everything here is the verbatim record: build rounds, Brad's review verdicts, and engineering findings. Organized by homepage section, oldest first within each. Add future session notes HERE (dated, one entry per session or round); `tasks.md` keeps a checkbox plus a one-line status.

Binding engineering rules discovered during these sessions are also recorded in STYLE_GUIDE.md; when in doubt the STYLE_GUIDE version governs.

---

## Phase 2 groundwork (0a to 0d)

Groundwork notes (2026-08-21): dev-only test bed at `app/dev/styleguide/page.tsx` (404s outside development, not in sitemap.ts) renders every primitive on all four theme grounds; verified 375/768/1280/1536 plus reduced motion, no horizontal overflow, no hydration errors. Site favicon is still missing (404s); needs a real icon asset in a later session.

## Homepage restructure: brief-first (2026-08-23)

Restructured 2026-08-23 with Brad: the old flat section list risked a one-shot generic build. Every home spec in `project-sections/home/` was rewritten into a reference-grounded design brief v2 (the old specs pre-dated the scrape and cited only Youtech/Scorpion; three contradicted STYLE_GUIDE.md). Locked decisions from that session: hero is statement-over-panel; problem/solution/services go editorial + bento (statement + numbered ruled rows, unequal bento, ruled index tables); motion is the specced system only with the scrubbed portal set piece deferred until real assets; build runs in the paired phases below, one phase per session, hero first.

---

## 2A Hero + 2B Nav

2A + 2B round 1 built 2026-08-23; Brad rejected it in review as generic conversion-page design. Round 2 rebuilt the same day against the corrected direction (briefs now v3; STYLE_GUIDE "direction pivot" changelog entry): hero is the scroll-driven film takeover with no CTAs, nav is a two-pill bar with a full-screen overlay index menu. Round 3 (same day): Brad approved the nav direction; hero takeover reworked per his notes into the pen-pull choreography (2.hero.md v4: ink line traces from the circled words and pulls the film in as a tilted sheet from bottom-left; rails removed from the hero; 360vh scroll room). Lusion.co studied live via wheel-driven capture (frames preserved in `project-sections/reference-images/lusion/live-scroll-capture/`).

Checked off 2026-08-23 with Brad as v1. His verdict on the hero set piece: direction right, execution "clunky and cheap, fragmented"; the Framer Motion sheet entrance is below the bar. **Queued for the next session: rebuild the hero set-piece rendering on a WebGL canvas** (bent/morphing video plane, lusion-style; three + r3f already sanctioned in PROJECT_REQUIREMENTS.md) — see the "Next iteration" block in 2.hero.md. Brad also plans his own nav iteration pass and a decision on the open area above the hero copy. Implementation notes: homepage at `app/(marketing)/page.tsx`, nav in `app/(marketing)/layout.tsx` (keeps funnels nav-free later); shared code: `Pill` (CSS in globals.css), `FramedMediaPanel` (kept for the portal section; hero now renders its own full-bleed media), `TitleAssemble` (built, currently unused); `ClipReveal.onMount`, `RoughAnnotation.staticRender`. Placeholder film: `public/media/hero-poster.jpg` + seamless `hero-loop.mp4` (ffmpeg breathe, 353KB); real 4K film is the one-line `HERO_VIDEO` swap in Hero.tsx. `/results/` stub added. shadcn navigation-menu/sheet/accordion were installed for round 1 and are unused after the pivot; leave for later reuse (footer accordions, dialogs). Verified round 2: 375/768/1280/1536 no overflow, reduced-motion static composition, prod build green. Favicon 404 remains (2J). Open with Brad: "Let's Talk" bar pill label vs copy-rules approved list; optional polish: luminance-sensing nav over the settled film, sound toggle when a scored film exists.

### Hero v5: WebGL set piece (2026-08-23)

Hero v5 session (2026-08-23, same day): rebuilt the set piece on WebGL per the queued note, then reworked it twice more on Brad's live feedback. Final form (2.hero.md v5; STYLE_GUIDE "hero v5" changelog): glass cube with `--acc` core floats above the copy (Brad's addition: the 3D brand object; his Spline reference screenshots never reached the agent, look pending his sign-off), swoops behind the text on scroll, flattens to a glass pane, film develops over it in ink duotone, balloons with a per-vertex cloth bend into a framed 4vw panel (full-bleed settle retired). Ink line CUT. Canvas actors ease toward scroll via a damped follower (5.5/s); wrapper grew to 320/480vh. three + @react-three/fiber installed (drei deliberately not; PROJECT_REQUIREMENTS updated). New files: `components/sections/home/HeroCanvas.tsx` (lazy chunk). Verified: 375/768/1280/1536 no overflow, reduced motion = static framed panel with zero canvas, prod build green, 3D chunk lazy and off the LCP path. OPEN: 3D chunk ~232KB gz vs the 200KB budget line (three core growth; Brad to accept or fund slimming); cube aesthetic sign-off; v6 idea queued: cube as persistent companion following the page (needs page-level fixed canvas). Playwright headless now launches with `--enable-gpu --use-angle=d3d11` so glass/transmission screenshots reflect real rendering.

### Hero v5.1 (2026-08-23)

Hero v5.1 (same day, Brad's live review of v5): smoke core replaces the solid block (opaque hashed-discard shader wisp; solid core refracted into "multiple cubes"), ior/thickness calmed, spin halved, swoop stretched (timeline in 2.hero.md v5.1; follower 4.5/s), headline raised beside the cube + statement de-collided (was overlapping at 2560), and the held card beat gained the side text block (DRAFT copy flagged; "See the Results" rule link). Verified again at 375/768/1280/1536/1920 + reduced motion. Still open for Brad: side-text copy, cube look sign-off, bundle overage call.

### Hero v6: the cube companion (2026-08-23, built with 2C + 2D)

2C + 2D built 2026-08-23 (one session, per Brad's ask) together with **hero v6: the cube companion** (the queued page-level canvas). The hero canvas moved to a fixed page-level layer (`components/sections/home/HomeStage.tsx` + `HomeCanvas.tsx`; `HeroCanvas.tsx` deleted); the hero wrapper grew to 374/560vh and its last 1/7 is the REFORM beat (film panel un-balloons back into the glass cube, transmission glass crossfades to alpha glass so the real page shows through it), then the cube travels the new sections as a waypoint companion (right side through trust, quarter-turn ticks per problem row, dives behind the solution bento, climbs the services rail, dissolves; mobile gets a short fade-out over trust). Waypoints live in HomeCanvas and extend per section in 2E/2F. New shared: NumberedRuledList, RuleLink, RuledLinkTable (+ `.row-fill` fill hover in globals.css), BentoPanel; SeparatorIn gained a delay prop; Section gained an `anchor` prop (data-cube-anchor). Verified: prod build green, 375/768/1280/1536 no horizontal overflow, reduced motion = zero canvas + static marquee grid + settled sections. OPEN with Brad: trust tiles are text wordmarks until real partner logo SVGs land (`[PLACEHOLDER]` in TrustMarquee.tsx); the 15 service links use the sitemap.md slugs and 404 until Phase 4; companion path/material is a first pass for his review (same open cube-look sign-off as v5.1); bundle overage call still pending.

### Hero scroll checkpoints (2026-08-24)

Hero scroll checkpoints (2026-08-24, Brad's ask): stopping a scroll mid-gesture could park the film sequence half-played. Added `components/motion/useScrollCheckpoints.ts` (idle-settle: after scrolling stops mid-beat the page glides to the rest beat the gesture was heading for; any input cancels it; off under reduced motion; STYLE_GUIDE 7.4 entry + 7.1 amendment). Hero rest beats: statement 0, held card 0.60K, settled panel 0.98K, release 1. Verified with a Playwright behavior suite at 1280 and 375 (commit forward, settle back under 22%, upward-direction settle, wheel interrupt, reduced-motion no-op): 15/15 pass.

### Smooth scroll instrument (2026-08-24)

Smooth scroll instrument (2026-08-24, Brad's review of the checkpoint build): he asked for the Obsidion/lusion feel, damped scroll with its own scrollbar instead of raw native wheel steps. `lenis` added (PROJECT_REQUIREMENTS.md; 7.1 lock superseded, STYLE_GUIDE changelog "smooth scroll instrument"): `components/motion/SmoothScroll.tsx` mounts Lenis in the marketing layout plus the right-edge scrollbar instrument (drag + click-to-jump, fades when idle, desktop fine pointers only; native scrollbar hidden only while Lenis is active). Touch and keyboard stay native; reduced motion never mounts it. Nav overlay pauses it (stop/start + data-lenis-prevent). Checkpoints retuned (commit 15%, glide 600 to 1400ms) and glide writes route through lenis.scrollTo so wheel input continues cleanly from a cancelled glide.

Round 2 (same day, Brad's review): checkpoints still read as hurdles and the thumb scrollbar looked basic and overlapped the nav. Retune: commit 6%, idle 100ms, glide ~550ms/viewport clamped 500 to 2200ms; settled-panel rest moved 0.98K to exactly K (dead zone before the reform made that boundary sticky). Scrollbar v2 = the progress rail (STYLE_GUIDE 7.1 changelog "progress rail"): hairline below the nav with "+" caps, accent fill, quarter-turning 7px brand square on the leading edge; dims at rest, drag-to-scrub 1:1, press-to-jump. Verified: 32/32 behavior checks (both suites) at 1280/375 + reduced motion, rail screenshots clear of the nav.

Round 3 (same day, Brad: direction approved, three notes): rail shrunk to 62svh centered and made display-only (pointer-events none, no drag/click/cursor); the between-beat hang fixed by firing the settle on Lenis velocity decay (<~2px/frame after 120ms input quiet) instead of waiting out the lerp tail's event silence; glide quickened to ~450ms/viewport clamped 450 to 1500ms. Measured flick-to-parked ~1.1s at 1280 including the beat playing. 34/34 checks pass. Remaining Brad question if the feel is still off: whether "each fold goes to its own section" means extending checkpoint snapping to the sections BELOW the hero (trust/problem/solution/services scroll natively today); the hook generalizes if so.

### Hero card-beat headline (2026-08-24)

Hero card-beat headline (2026-08-24, Brad's ask): the held-card beat gained "Proof before promises." at display scale, swinging in from the top left with per-line lag + clearing blur (2.hero.md v6.1; STYLE_GUIDE changelog entry). Side-text rule link retargeted to /about/ as "How We Work" (featured work will land directly below the hero, making a results link redundant there). BOTH the headline copy and the link label are DRAFT pending Brad. Verified 375/768/1280/1536, no horizontal overflow, typecheck clean; reduced-motion static path unchanged (it never had the card beat).

---

## 2B2 Featured work

Featured work session (2026-08-24, Brad's ask): a lusion-style featured work grid directly after the hero film, 6 projects, each opening a case study page. Build 1 (rails + Nº label + mono indexes inside the 1200 container, normal section after the hero) was REJECTED live: "compacted... clunky... too mechanical"; he wants the fun portfolio feel at lusion's scale, sitting AT the hero's release rest beat (his screenshot: the blank viewport with the cube is exactly where the checkpoint parks). Build 2 per the rewritten brief (2b.featured-work.md v2): the section pulls up into the release beat (-35svh mobile / -60svh desktop; canvas path only) so "Featured work" at `--text-display` rises while the film reforms into the cube; edges at max(5vw,40px), no rails/hairlines/Nº (section is unnumbered, Problem stays 002), 3:2 darkpanel media cards with `--text-menu` Bluu titles and the lusion arrow-leads-title hover, dot-separated mono tag rows, centered "See All Results" pill. New: `lib/featured-work.ts` (6 entries, ALL `[PLACEHOLDER]` titles/media until real case studies; tags are real service names), `/results/[slug]/` skeleton pages (template order, noindex, static params so no card 404s). Hero's post-wrapper InfoBar REMOVED (the section rises where it sat). Cube: `work` anchor waypoints (header right → grid center seam → trust handoff); mobile fade-out moved from trust to the work top. 8.case-studies.md marked superseded (grid half; 2E becomes proof band only, PENDING Brad's confirm). Verified: 375/768/1280/1536 + reduced motion, no overflow, no console errors, typecheck clean, release beat shows the headline on desktop and mobile. OPEN with Brad: placeholder card treatment until he supplies 6 real clients + media, support copy sign-off, cube seam path, the uppercase support paragraph vs copy-rules' ALL-CAPS rule.

Round 3 (same day, Brad: placement direction approved with two notes): the release rest beat parked the headline mid-screen with a large gap above, and the cube ended on the LEFT (the reform's CARD_CENTER), clashing with the headline and card boxes. Overlap deepened to -48svh/-72svh + pt-[2svh] (headline now upper-third at release; depth is bounded by the settled film panel above), and the heroEnd waypoint retargeted right-of-center (0.28, 0 desktop; 0.2, 0.16 mobile, scale 0.82) so the companion follower swims the cube right as the page parks. Verified by DOM+pixel measurement at 1280/375: cube core centered in the open band between support text (bottom 322px) and card row (top 468px) at 1280; above the headline on mobile; full suite re-verified after the margin change (no overflow, no errors). Brad defers all copy review to a single later pass.

Round 4 (same day, Brad: "absolutely stunning" with four transition notes): (1) settled film panel shrunk, margin 4vw → max(6vw, 96px) (canvas panelRect + hero DOM meta/marks/no-webgl clip), because 4vw ran the panel top under the 72px nav; (2) the featured work header no longer clips through the film: on the canvas path it is scroll-GATED (hidden through the film beats, quick load-in — baseline rise + support fade, house ease — once the section climbs clear at ~46svh desktop / ~61svh mobile top, re-arming on scroll-back so text can never sit over the film; thresholds are breakpoint-aware since mobile's shallower pull-up parks the header lower); (3) cube nudged up-left, REFORM_END (0.24, 0.03); (4) the reform's left-then-right dogleg fixed: the shrinking panel's target now slides from CARD_CENTER to REFORM_END (right-of-center) during R_SHRINK, the cube materializes riding that same path and eases to its settle scale (R_SETTLE), and heroEnd mirrors REFORM_END exactly, so the panel shrinks down to the RIGHT into the cube's parked spot and the takeover is motionless. Verified by DOM+pixel measurement at 1280/2560/375: header hidden at the settled beat everywhere, panel top 97/154px (clear of the nav), release composition clean (cube core inside the support-to-card band at 1280/2560, above the headline at 375), no overflow, typecheck green.

Round 5 (same day, Brad's markup screenshots: cube position + the panel morph ask): (1) settled cube moved up-left, REFORM_END (0.24, 0.03) -> (0.14, 0.22) (the whitespace band between the headline and the support column, per his red box), heroEnd mirrors it, so the cube no longer clips behind card 02. (2) The v2 center-seam grid journey replaced by the WORK PANEL MORPH (spec v3): scrolling into the grid, the cube dives to the grid's top edge, unwinds face-on, flattens, floods --acc blue, and is swallowed by ONE DOM panel that grows out of its slab behind all six cards (bar-first wipe: scaleX leads in the exposed band, scaleY sweeps down behind the cards; uniform scale read as a gutter strip); settled, the panel wears a white boiling RoughAnnotation box squiggle (new variant; long edges chopped ~240px runs; component now measures offsetWidth/Height so a transform-scaled ancestor can't shrink the drawing); off the far edge everything reverses and the cube reforms from the panel's bottom edge, resuming the journey. Shared clock lib/work-panel.ts measured off untransformed [data-work-panel] by BOTH the canvas Tracker and the section's useAnimationFrame (enter/exit windows 0.5vh->0.12vh sit high because the section overlaps the hero tail; morph must be 0 at the release rest). Caption ink crossfades --sec-ink/--sec-mid to on-accent white via color-mix over a --wp motion var (never white on bare paper). Cube follower pins to the live target while swallowed so the reverse morph starts clean; glass clearcoat reset added to the hero branch. Verified 1536/768/375 x 6 scroll states + reduced motion (static panel, drawn border): no overflow anywhere, typecheck green. Post-exit the cube rides behind the problem strip's filled panel (accepted, region-pivot note).

Round 6 (same day, Brad's review of the round 5 morph: "good, not great... the white lines clash... no transition animation that ties us together... no scroll checkpoint... feels cheap and unfinished"): three mechanics changes, spec now v4. (1) Edge squiggle: the box annotation moved ONTO the panel edge with rounded corners and is stroked in the panel's own --acc (new RoughAnnotation stroke prop): only the wobble escaping onto the paper shows, so the edge reads hand-drawn instead of a contrasting inner line; it lives outside the scaled visual so it never distorts. (2) Canvas-led handoff: the slab floods blue on the EMISSIVE channel (lit PBR base can't match a CSS hex) and stretches in-canvas to the exact bar the DOM panel swaps over (PANEL_BAR_VH 0.14vh x panel width; follower damping ramps 4->28/s near the handoff; DOM side then only sweeps scaleY) — one continuous object, no fade-in. Fixed by screenshot: the face-on unwind must round to the nearest HALF turn, not quarter (a side landing flattens to a sliver). Top outset grew to clamp(64px,10vh,128px) so the stretch plays in whitespace. (3) useScrollCheckpoints gained dynamic bands (absolute-scrollY ranges resolved at settle time); the two morph bands glide idle parks to free-cube or settled-panel — verified live in the shoot (mid-band shots glided to the correct boundary per approach direction; screenshots of mid-states use a held pointerdown to suppress the settle). ENTER start pulled to 0.45vh so the band clears the hero's release checkpoint (~0.49-0.53vh) on all measured viewports. Re-verified 1536/768/375 x 8 scroll states + reduced motion: no overflow, typecheck green. Brad accepts the blue-heaviness for now: real assets should carry it; revisit after media lands.

Round 7 (same day, Brad: "very close... combine all of those into one animation with one checkpoint... just doesn't do it so fast"; plus a white frame appearing right-side when scrolling up from the release): spec v5. (1) One-band choreography: the release rest is RETIRED on the canvas path (Hero.tsx CHECKPOINTS_CANVAS = [0, 0.6K, K]; fallback keeps 4 rests) and the morph's enter band starts exactly at the settled-film K rest (workPanelBands enterStart, computed in FeaturedWork from the hero wrapper), so film-settled and panel-settled are the ONLY rests between the media player and the blue box; parking anywhere between glides the full reform-float-morph (verified live: an idle park mid-morph after an up-scroll glided ~100vh back to the settled film; the boundary rule - two checkpoint scopes must share one scrollY - added to 7.4). (2) Slower pacing: ENTER/EXIT widened to [0.55, 0.05] (0.5vh), handoff 0.65 -> 0.75, sub-beats spread (dive 0-0.5, flatten 0.25-0.55, flood 0.3-0.6, stretch 0.55-0.75, sweep 0.78-1, captions 0.8-0.95, border 0.94+); the morph now begins during the reform tail so the release composition is a pass-through frame (~0.1 dive weight, verified it still reads). (3) White-frame fix: reform glass alpha rides slab thickness (x0.25 flat -> 1 as it thickens), so the up-scroll reads cube-melts-to-film; verified at reform c=0.65 held: soft faded card, no bright slab. (4) Mobile regression guard: with no cube on mobile the DOM bar leads its own grow (opacity 0.45-0.55, scaleX 0.14->1) instead of popping at the handoff. Re-verified 1536/768/375 x 12 scroll states (pointer-hold suppresses the settle for mid-frames) + reduced motion: no overflow, typecheck green.

Round 8 (same day, Brad: round 7 "didn't work... one tiny scroll... blasted past the cube spinning animation entirely... it was just better before"): spec v6. Root cause of round 7's failure: the film-to-panel mega checkpoint band auto-glided ~100vh off one wheel notch (COMMIT 6% + 1500ms glide clamp = warp-speed autoplay); reverted (hero keeps its 4 rests everywhere; 7.4 gained the hard lesson: a band must never span more than its own beat; continuity = pinned scroll, not glides). THE FIX: the hero's own architecture applied to the section: header + grid are sticky (top 6svh) inside [data-work-stage] with a 110svh spacer runway (70 mobile), and the whole morph is scrubbed 1:1 over it: ascent, showcase spin (+1.3 rad beat), dive 0.28-0.58, flatten 0.42-0.66, flood 0.46-0.7, stretch 0.66-0.8, handoff 0.8, sweep 0.82-1. Pin clock is pure rect math ((child.top - wrapper.top)/(wrapperH - childH)); settled on fallback (runway renders 0). ENGINEERING GOTCHAS (binding, in STYLE_GUIDE changelog): (1) sticky offsets are constrained to the containing block's CONTENT box - a runway as wrapper padding gives the pin zero room and silently never sticks (found live: computed position sticky, zero displacement); (2) a pinned section's data-cube-anchor rect keeps moving during the pin, so waypoint fracs advance mid-pin and pin blends must own the cube. Scroll-up white-box fix: REFORM_END back down to the round-4 landing (0.2, 0.02) so the shrinking film card develops where the panel balloons from; the cube ASCENDS to the float spot (0.14, 0.22) via a work waypoint at frac 0.16 in the release-to-pin travel (also keeps it clear of card 02 at the pin-start rest, which otherwise swallowed it - found by screenshot). Verified 1536/768/375 x 13 states + reduced motion + live probe of the pin (pinTop 52px while stage at -898) and of a mid-runway park gliding to the runway edge: no overflow, typecheck green.

Rounds 9-11 (2026-08-24, commits `32b1eff` / `0e5a995`): round 10 moved the morph to a clip-path one-object treatment (no corner distortion, squiggle rides the same edge); round 11 pinned at the release comp with text exit, center-stage turntable (four turns then, one since round 12), 240svh runway. **Approved 2026-08-24: Brad is happy with the current state of the hero, the film section under it, and featured work.** Blue-heaviness stays parked until real assets.

Rounds 12-13 (2026-08-25, Brad, scrolling from load: the cube "rotates just quite a bit too much... it spins extremely fast, like dozens of times", then on the round-12 result "it turns too little now... go for three turns"): WORK_TURNS 4 to 1 to 3, spec v11. Diagnosis was measured, not eyeballed: a Playwright probe recorded group.rotation.y against window.scrollY through a natural wheel scroll and showed the turntable running 0 to 28.0 rad between scrollY 4061 and 5470, which is 4.45 turns in ~1400px (4.3 turns per viewport height, peaking near 4.7 turns per second). Round 11 honoured Brad's "four or five times" as a COUNT and never checked the RATE the runway gives it. Everything else on the page measured calm: hero rest holds under 0.12 turns, the solution sweep adds 2.2 rad, the portal turn ~1.4 turns. Round 13 shipped 3 turns: 3.00 turns over ~1480px, ~1.75 turns per viewport height, ~4.6 turns/sec sustained. Worth remembering that 4 to 3 bought only ~16 percent off the peak speed (smooth() front-loads the motion into the middle third), so if it ever reads fast again the lever is the ease or the runway length, not the count.

Round 14 (2026-08-25, Brad, with stepped screenshots of the media-player-to-cube transition: "it shrinks down into this weird little box and then to the cube... an additional step in there that shouldn't be there... should just go zoop down from the media player into the cube right away and start spinning"; and scrolling up "there's not enough delay... turns into this white box first in the bottom right corner and then zooms up into the media player"): the hero REFORM rebuilt as one continuous contraction. Four mechanisms, all in HomeCanvas.tsx + Hero.tsx. (1) The parked slab was real: R_SOLID [0.4,0.58] and R_UNFLATTEN [0.58,0.88] abutted, so ~13vh of scroll showed only a pale card-size glass rectangle crossfading, then a SECOND shrink card-to-cube; the beats now overlap (SOLID [0.28,0.5], PANE_FADE [0.5,0.65] late and steep, UNFLATTEN [0.46,0.82], SETTLE [0.46,0.86], GLASS_MIX [0.62,0.88], ASCEND [0.8,0.96]) so the object is always shrinking + thickening + crossfading at once. (2) ROOT CAUSE of the white-box read both directions: the crossfade was never a crossfade: the transmission glass writes depth and its transmission buffer EXCLUDES transparent materials (the 2.hero gotcha), so the slab ERASED the film wherever they overlapped and showed blurred backdrop instead: the film pane is now depthTest:false (canvas-topmost overlay) and its rect rides the SAME sEff/flat contraction as the glass (skinW/skinH in FilmPane mirror GlassCube's scale math), so the dark ink skin stays glued to the thickening cube and melts off it. (3) Spin-in: rotation was hard-zero for the whole reform, then companion mode snapped it on in a ~2px scroll window with a step that grew with time-on-page (target included clock.elapsedTime*0.1); new R_SPIN_IN [0.59,0.95] winds the cube into REFORM_POSE (0.42, 0.68, -0.08 = the companion engage pose, extracted so the sides cannot drift) plus REFORM_SPIN_TURNS (1) whole turns; the companion idle turn is now engage-epoch-relative ((t - t0)*0.1) with a whole-turn bias (round to nearest 2pi at entry), and companion EXIT banks the sub-half-turn residual (wrapPi) into a decaying, spinIn-gated add, so both handoffs are stepless at any dwell time (measured per-frame |dry| at the boundary: 0.008 down, 0.05 up, 0.03 after a 6s dwell; whole turns drop pixel-identically by cube symmetry). Guardrail pair when tuning: R_SPIN_IN[0] >= R_PANE_FADE[1] - 0.06 keeps the skin near face-on while visible. (4) Reverse pacing: the hero band [K,1] spans the whole 568px reform but glided at the DEFAULT 450ms floor (the work morph got 2600 in round 9; the hero never did): CHECKPOINTS split into two hook instances (MAIN [0, 0.6K, K] at default pacing, REFORM [K,1] at glideMsPerVh 2600 / max 3000) so the reform auto-completes in ~1.7s desktop (~1.0s mobile) both directions, measured 1423ms wheel-to-rest on a committed reverse; Act-1 rests keep the approved feel (the uniform 550/2200 tune once read as sluggish). Also unified the clocks: mix + PaperBackdrop moved from raw to sp (color could whiten before the shape melted on a fast reverse); the backdrop's retire guarantee now rides the companion gate, which is the actual invariant. Verified: stepped Playwright captures 1536 + 375 both directions (13/9 steps, mouse held to park), 768 + 1280 spot frames, reduced motion (static hero, no canvas), glide timing, dwell handoff, work morph + turntable untouched. KNOBS for the next tuning round: the R_* block, REFORM_SPIN_TURNS (0..2), the 0.25 flat alpha cap, GLASS_MIX start, glideMsPerVh 2600. The __cube debug probe stays in until Brad signs off this round.

---

## 2D services (rejected build) and the region pivot

**2D review, 2026-08-24 (Brad): REJECTED as built; 2D unchecked. Expect a ton of iteration over multiple sessions.** His verdict on the services section: "very generic SaaS template". It drops straight into a listing of things in a poor format: bad copy, not interactive, boring, a poor way of displaying the information. What it must become: the page should LEAD from the unique showcase sections above (hero film, cube) INTO a more unique section, with a more artistic approach, like the featured-work treatment above it. Calibration he gave: NOT extremely creative, not a portfolio, but clearly a step more unique and designed than what shipped. Reference calibration: the current result leans too much toward "the UTEC inspiration" (his word; most likely Youtech, the conversion-agency reference — CONFIRM with him which reference he means before rebuilding); pulling SOME inspiration from it is useful, but dial it back. Working plan for the rework sessions: confirm the reference read, rewrite `6.services.md` to v3 with the new direction FIRST (brief before build, per the phase loop), then rebuild with interactivity and better copy, and expect 1 to 2+ focused iteration rounds with Brad. Note: his critique named the services region specifically; 2C (trust + problem) was not flagged in this review, and the solution bento was not explicitly ruled on — re-confirm both when reviewing the services rework.

**Region pivot, 2026-08-24 (Brad's session, follows the 2D rejection): the whole lower homepage moves to the Youtech open layout.** Brad confirmed "the UTEC inspiration" = Youtech, and inverted the earlier dial-back read: he wants the region MORE like Youtech's open flow, not less. Everything from FeaturedWork down drops the grid system (no GridLines rails, no 1200 Container, no Nº labels, no numbered/ruled rows) and moves to the FeaturedWork EDGE width (`px-[max(5vw,40px)]`, now shared via `lib/layout.ts`). New order: ProblemStrip → Solution (3 widget cards) → Search (Claude-composer typing mockup, new section) → Services (3 pillar cards) → Testimonial → ProofBand (dark stats) → TrustMarquee (moved to region bottom). Locked decisions: solution card titles stay Youtech's for now ("No Long Term Contracts" / "Transparency" / "Full Approach", Brad expects to swap at least one); testimonial + proof band build now with `[PLACEHOLDER]` content; cube keeps current behavior with minimally re-mapped waypoints (Brad retunes the journey during the build process, task 2K); "AI" stays a banned word so the Search section names products instead. Fresh scrape finding (probe in `project-sections/reference-images/youtech-agency/2026-08-24-widgets/`): all four Youtech "animated widgets" are looping MP4s (~3s cards, 6.7s Claude composer); ours rebuild them as real HTML/CSS loops (first `@keyframes` in the project, `wgt-*` block, reduced-motion gated). 2C stays checked as built, but its problem section is superseded by 2D-R and the trust marquee's position moves in 2E-R. BentoPanel becomes styleguide-only after this rebuild: deletion candidate unless a later phase claims it. The plan file for the pivot session carries the full wireframes.

### Ambient square field (2026-08-24)

Ambient square field (2026-08-24, Brad's ask): the page read flat between the set pieces, so the background gained a quiet undercurrent: `components/motion/SquareField.tsx`, nine 1px ink square outlines at 4 to 8% opacity drifting at ocean speed (slow spin, scale breathing, two-sine wander, per-square parallax depth so each section arrives at a reshuffled composition, scroll-velocity stir, vertical recycling). Mounted by HomeStage on a fixed layer at z-1, under the cube canvas and all ink, over grounds and rails (layering contract updated in HomeStage + STYLE_GUIDE 7.4 entry + 7.8 row + changelog). Zero canvas, one rAF, no new dependencies. Reduced motion: static seed composition. Tuning dial: the SQUARES table (positions/opacities) in SquareField.tsx. Round 2 same day (Brad: squares flashed at the hero on refresh, then vanished; fine below): the loading canvas's opaque paper backdrop was covering the SSR-painted field. The layer now renders at opacity 0 and scrubs itself in over the hero's reform band (heroReveal prop), synced with the backdrop retiring, so the field develops in as the cube releases; no-canvas branches ease in on mount. Re-verified: hero-top opacity 0, below-hero opacity 1 + animating, reduced static + visible, 375/768/1280/1536 no overflow. Round 3 same day (Brad: squares must SHOW in the hero, my reveal-at-reform call overridden; plus giant off-frame jellyfish squares and thinner/wispier strokes): field now rides at z-6 above the canvas backdrop through the statement/film beats and drops to z-1 at the reform (heroStage prop; companion still passes over the field below the hero); recomposed to ten squares: eight tumblers at 3 to 5% opacity plus two off-frame giants (680/820px, ~2.5%, 0.75px stroke, near-still, slow clocks). All 14 checks green at 375/768/1280/1536 + reduced.

---

## 2D-R Lower region rebuild

2D-R build session 1 (2026-08-24, per `handoffs/2026-08-24-region-rebuild-session-1.md`): groundwork + ProblemStrip + Solution shipped, at Break B awaiting Brad's screenshots review; Search and Services pillar cards remain (next sessions). Groundwork: shared `EDGE` in `lib/layout.ts` (FeaturedWork refactored to import it, zero visual change); page order now Hero, FeaturedWork, ProblemStrip, Solution, Services (rejected build stays until its session), TrustMarquee at the region bottom; HomeCanvas anchors and waypoints minimally re-mapped to the new document order (search/proof listed early and filtered until built; steps4 untagged but kept; the dissolve now lands on trust; values are placeholder-grade for Brad's 2K retune); globals.css gained the `wgt-*` keyframes block (the project's first @keyframes) with data-play pause gating and the reduced-motion kill; STYLE_GUIDE 4.5 open region + 6.11 widget card + 7.10 looping vignettes + section 9 rewrite + changelog. ProblemStrip built per 4.problem.md v3 (old Problem.tsx deleted; heading set in Bluu at h2, the brief's build-test call). Solution rebuilt per 5.solution.md v3: statement headline, intro + pill pair (pills full width at mobile), three equal cards with pure-CSS widget loops (CalendarWidget 4.5s three-beat cross-fade, real month names, no year; TaskListWidget 5s sequential check-off with one fade-reset; ChannelBarsWidget 5.5s staggered grow-hold-retract in acc/acc2/mid). Build finding worth keeping: an absolutely positioned strike bar breaks on wrapped titles (caught at 768's narrow cards); the strike is now a duplicate line-through text layer fading in, opacity-only, so every wrapped line strikes. Verified on the prod build: 375/768/1280/1536 plus reduced motion (settled frames confirmed: static January, both rows checked, grown bars; probe shows zero data-play, zero animating elements), no horizontal overflow at any width, loop beats confirmed frame by frame (February/Mon 4, March/Fri 1, check-check-reset), offscreen pause confirmed (at 375 only the in-view widget plays), tsc and prod build green, /dev/styleguide untouched. Scroll-through at 1280: the cube follows the remapped path (hands off right of FeaturedWork's closing pill, passes half then fully BEHIND the ProblemStrip panel, screenshotted for Brad as requested, crosses left over Solution's exit, climbs Services, dissolves at trust); no console errors beyond the known Phase 4 service-link prefetch 404s. OPEN with Brad: all copy DRAFT for his single later pass (card titles stay his locked Title Case wording); the ProblemStrip heading face call (Bluu); waypoint retune queued as 2K. Next session: Search + ChatInputWidget (`5b.search.md`).

2D-R review, 2026-08-24 (Brad): **ProblemStrip and the Solution section with its three widget loops APPROVED** ("super happy" with both). Services rejection stands: the current build reads as a placeholder to him; he has a new direction he wants to execute, so capture it into `6.services.md` v4 with him BEFORE that rebuild (brief before build). Search direction CHANGED before its build: the mockup must be a one-to-one ChatGPT recreation, not a tokens-native Claude suggestion — if it reads as a made-up composer it looks weird, so it must look exactly like the real product — and it keeps Youtech's camera choreography (zoomed into the input as typing starts, zoom out as the query nears completion) while cycling several queries. `5b.search.md` rewritten to v2 with the new direction; section built the same session (see the search build note below).

### Search (2026-08-24)

Search build (2026-08-24, same session, per 5b.search.md v2): `Search.tsx` + `widgets/ChatInputWidget.tsx` shipped, page order now Hero, FeaturedWork, ProblemStrip, Solution, Search, Services (rejected build still in place), TrustMarquee. The mockup is a one-to-one ChatGPT composer (white card, #e3e3e3 hairline + soft shadow, #0d0d0d ink, "Ask anything" placeholder, plus circle, mic, black voice circle whose waveform swaps to the up arrow once text exists, neutral system sans) rendered aria-hidden inside a bg-paper rounded-24 viewport panel; the camera choreography is a transform scale on the inner stage (zoomed 2.1 into the input for the rest + typing, easing out at 60% of the query over 1.15s house, 1.9s hold on the full composer, back in for the next query), cycling the 3 spec queries ~7s each via a JS state machine (the first JS-driven widget; sanctioned in the STYLE_GUIDE "depicted product UI" changelog entry along with the hard-coded-values exception for depicted products). Only the caret blink is a keyframe (`wgt-caret`, shared block, steps so it cuts). Reuses the `.wgt`/`data-play` gate: SSR first paint is the deterministic zoomed-in rest, timers only run in view. Verified on the prod build at 375/768/1280/1536: no horizontal overflow anywhere, zero console errors, loop confirmed cycling into query 2, zoomed-typing and full-composer-hold states both match the Youtech reference framing, closing line + CTA compose under the panel; reduced motion probe: static zoomed-out composer with query 1 complete, arrow lit, ZERO animating elements; typecheck + prod build green. The search cube waypoint activated with the section (anchor="search"); the glass cube parks near the panel's top-left edge at 1280, first-pass grade like the rest of the journey (2K retune). OPEN with Brad: all copy DRAFT (headline is still Youtech's line verbatim); the stats paragraph is a visible `[PLACEHOLDER: needs source]` (cut at launch if no source lands); zoom origin/scale tuning to taste on review. Remaining in 2D-R: Services rebuild, direction capture with Brad first (6.services.md v4).

Search round 2 (2026-08-24, Brad: "almost came out perfect first round", two notes): (1) the mockup was too small/boxed; the panel now runs the FULL EDGE width with a taller viewport (320px at 375 up to 560px desktop, composer 720px, text 18px) for the magazine/portfolio read (5b.search.md v2.1). (2) All v2 draft copy replaced wholesale by Brad's own wording, verbatim, no longer DRAFT: headline "Your customers are not only searching on Google anymore.", two AI-shift support paragraphs, closing "We help your business show up in search results and in the answers people get from AI.", CTA "Find Out Where You Show Up" (his label; longer than the button-rule 2-to-4 words, his call). His copy says "AI" by name: recorded as a scoped copy-rules exception (referential use where AI is the subject; the ban stands for describing our own services), superseding the region-pivot "name products instead" lock. The stats placeholder paragraph is gone with the old copy. Re-verified on the prod build: 375/768/1280/1536 no overflow, zero console errors, loop cycling, reduced motion static with zero animating elements, typecheck + build green.

Search round 3 (2026-08-24, Brad: "almost perfect", but the section ran past one viewport, too much gray padding around the composer): 5b.search.md v2.2. Section rhythm dropped to base `py-section-y`, panel heights cut to 240/300/360/380, composer widened to 820px/92% so it fills the panel, zoom eased to 1.9 (origin 23% 40%), stacked margins tightened. Panel keeps the round-2 full-EDGE magazine width. Measured section height now ~960-980px at all four widths (was ~1300+), no overflow anywhere, zoomed typing frame re-verified at 1536 and 375, prod build green. Awaiting Brad's screenshots review.

### Card sweep: the cube's solution passage (2026-08-24)

Card sweep session (2026-08-24, Brad's ask): the cube's solution passage rebuilt as the CARD SWEEP (5.solution.md v3.1): it descends the whitespace RIGHT of the card row and sweeps flat right-to-left under all three cards; each title draws a boiling `RoughAnnotation` underline as the cube crosses beneath it (Brad rejected a first bespoke smooth-sine underline mid-session: card ink must be the SAME rough scribble system as the hero circle / work panel box; three underlines in one viewport is his approved budget exception). Sync via new `lib/solution-sweep.ts` beats consumed by both the canvas (`solutionCards` anchor waypoints) and the DOM triggers in new `SolutionCards.tsx` (grid extracted from Solution.tsx; mobile lights per-card top-down, reduced motion renders settled). After the sweep the cube drops behind Search and stays hidden through ProofBand (both sections now `z-[6]` OCCLUDERS above the canvas, 5b.search.md v2.3 / 7.proof-numbers.md v3.4) and re-emerges at the trust marquee; the old search/proof riding waypoints are gone. Verified 1536 sweep beats + hidden passage + trust reveal, 375 per-card, reduced motion, no overflow, typecheck green. Exact sweep ys still placeholder-grade for Brad's 2K retune.

Rounds 2-4 same session (Brad live): sweep underlines became TWO-WAY (new RoughAnnotation `rearm`: draw as the cube passes, retract on scrub-back, replay every pass; opacity-snap kills the zero-length dash dot) after "the blue lines just are there the whole time"; solution got a deep bottom runway (`pb-[max(220px,28svh)]`) and the cube retimed to DIVE out of frame early and sweep while the cards are front and center (beats moved 0.56-0.76 -> 0.5-0.66) after "the animation takes too long... the card is already basically scrolling past"; the problem strip gained its [ ] BRACKET PAIR (new `outset` bracket geometry, slightly bigger than the panel; draws immediately on entry, persists; his round-3 correction of my cube-gated version; 4.problem.md v3.1); the trust eyebrow gained the hero-circle (padded box after "too tight... clipping the words", new `boilMs` 320 after "moving a little bit too fast"; 3.trust.md v2.6). All verified by DOM probe at 1536 (image reads were unavailable): brackets outset on all four edges + 89%-drawn 0.5s after entry + persisted at exit, underline beats one-by-one with the grid mid-viewport + clean retraction, circle clear of both the words and the marquee row.

Round 5 same session (Brad: "we're still missing the animation... it scrolls away too quickly. Let's stop the scroll with this in full viewport... unfreeze after the cube gets all the way to the left side"): the sweep became a PINNED RUNWAY (new `SolutionStage`: whole composition sticky over a 130svh spacer, measured sticky top; the standing pin-and-scrub rule applied). New shared `sweepPin` rect clock in lib/solution-sweep drives all three consumers; the cube's pin path is now PX-exact off the frozen grid's column centers, entering/exiting below the frame so waypoint handoffs are invisible; beats at p 0.3/0.5/0.7; the old grid-frac beats + deep bottom pad reverted (pad now max(140px,16svh)); runway is a free-park zone, no checkpoint bands; fallbacks collapse the spacer so the sticky never engages. DOM-probed at 1536: pin rect frozen through p 0.05-0.95 with a 104px lane below the row, beats one-by-one + reverse retraction on the runway, release un-sticks at p=1, mobile/reduced runway room 0 with per-card underlines intact, no overflow, typecheck green.

### Services rebuild: the spotlight index (2026-08-25)

Services rebuild (2026-08-25, closes 2D-R, per `6.services.md` v4): `Services.tsx` rebuilt as the SPOTLIGHT INDEX (the full 15-service catalog as editorial link rows under mono group eyebrows, plus a sticky hairline panel whose text morphs to the hovered service) and the cube's page-long journey now ENDS by docking in that panel's empty bay (`lib/services-dock.ts`, `[data-services-dock]` measured per frame; row hover flicks the cube with a spin impulse). Shipped in commit `beccb0c` with Search, the card sweep, and ProofBand. **Brad 2026-08-25: "pretty good."** Copy (headline, support, the 15 one-liners) is still DRAFT for his single later pass; group names stay locked per copy-rules. Remaining 2D-R work: none. The dock and its dissolve get retimed in 2F, which takes the cube on into the portal section.

---

## 2E-R Proof band + trust marquee

2E-R CLOSED 2026-08-25. The testimonial lockup is RETIRED from the homepage (Brad's homepage-close call, footer session); the pattern moves to interior pages once real testimonials exist. ProofBand SHIPPED 2026-08-24 (Brad's proof-band session): position changed by Brad to directly under Search with TrustMarquee moved directly beneath it (one extended trust block, Youtech frame 05; Services untouched, now after the block); built with ALL placeholder content (Brad drafting copy outside the session; media panel renders honest placeholder with aria-hidden player chrome), MetricBlock + `lib/metrics.ts` built (4 null-value slots; grid handles 3 or 4), `public/logos/` drop folder created for Brad's partner SVGs (wire via `src` on LOGOS entries). Cube waypoints re-ordered to the new document order (proof, trust, services; dissolve moved to the services exit; still placeholder-grade for 2K). Same day: Brad's TEST COPY applied verbatim (headline, two support paragraphs, "See how we help brands scale" rule link) with VISUAL PLACEHOLDER metric numbers at his request (launch gate in lib/metrics.ts: all four values must be replaced with sourced figures); 5 of 8 partner SVGs wired (Google Ads, Microsoft Advertising, Yext still text wordmarks). Round 2 same day (Brad's live notes): band trimmed (too tall/verbose: base rhythm, second support paragraph cut and held in 7.proof-numbers.md v3.3, media column 1.15fr; band ~730px at 1280, was ~900+); marquee tiles dropped for the open FULL-COLOR strip (3.trust.md v2.3, STYLE_GUIDE 6.4 logo item + 7.5 + changelog), hover pause retired, velocity boost now magnitude-only (never stops or reverses), amazon-ads SVG viewBox cropped in-file to its content bounds (was padded to ~23% height). Round 3 same day: google-ads/yext/klaviyo SVGs wired (Klaviyo confirmed into the strip by Brad supplying it; 9 logos, only Microsoft Advertising still a text wordmark; missing viewBoxes added to yext/klaviyo files so they scale instead of clip), and the marquee's wide-viewport dropout fixed (2 fixed copies ran dry after WordPress; copies now scale 1 + ceil(viewport / set width), wrap on one set's measured width; 3.trust.md v2.4). Round 4 same day: Brad's full SVG drop wired (24 logos, all SVG; text placeholders dropped on his call; Microsoft four-square mark stands in for Microsoft Advertising; list now spans ad platforms + build/ops tools, 3.trust.md v2.5; intake hygiene: kebab renames, anthropic + google-cloud viewBox crops). Still open (content, with Brad): copy lock, sourced metrics, real media, 3-vs-4 metric call. `lib/testimonials.ts` is no longer needed for the homepage.

---

## 2F Obsidion portal

Spec: `9.portal.md` v3, rewritten 2026-08-25 in Brad's portal session (v2 was pre-region-pivot and specced rails, the 1200 Container, a Nº label, and a 12-col split, all retired). Position: directly after Services. Reference scraped this session at his ask: paper.design (13 desktop frames + ANALYSIS.md in `project-sections/reference-images/paper-design/`), for how a product UI is presented as an object on the page. Four decisions locked with him: (1) the Obsidion window is a STRUCTURAL MOCK, full app chrome in HTML/CSS with every value a neutral skeleton bar, no digits, plus a visible `PORTAL PREVIEW` chip (matches the decisions.md honesty rule and paper.design's own placeholder state); (2) composition runs copy, window, features, CTA, so the window is the section centerpiece; (3) **the cube reforms INTO the window**, retiring the services-exit dissolve, built as a pin + runway with one shared clock (`lib/portal-window.ts`) reusing the work-panel architecture but a different handoff gesture (the slab becomes the window's top chrome bar and the body unrolls beneath it, so it does not repeat the featured-work waterfall); (4) ground is tint. Build order inside the session: static section first, reviewed with Brad, then the set piece.

2F build session 1 (2026-08-25, Brad's portal session): reference scraped (paper.design, 13 frames + ANALYSIS.md), `9.portal.md` rewritten to v3 with his four decisions, then the STATIC section built and verified. Shipped: `components/sections/home/Portal.tsx`, `components/sections/home/PortalWindow.tsx`, `lib/obsidion-preview.ts` (the swap contract: field names only, not one value in the file), wired directly after `Services` on the homepage. The window is a full app shell in markup, tint ground, ground ladder surf -> paper -> surf, `PORTAL PREVIEW` chip in the top bar, a second "Call log" window overlapping its lower left at `xl`, registration marks at the frame corners, and progressive disclosure inside it (tab strip at `sm`, rail + lead table + pacing panel at `lg`, second window at `xl`). Verified on the prod build at 375/768/1280/1536 + reduced motion: no overflow, chip visible everywhere, **zero digits inside the window** (probed), zero running animations at rest, typecheck + build green; only the known service-link prefetch 404s in console. Section height ~1410px at 1280, ~1784px at 375.

2F build session 1, part 2 (same day): **Brad reviewed the static window and approved it** ("scale of the window is great, looks good"), and made the asset call: the real portal arrives as **LIVE CODE, not screenshots** ("allows for better interaction and the way that the cube and everything can animate and play together"); the placeholder stands until then. That changed the architecture before the morph was built: `PortalWindow` became a SHELL (frame, chrome bar, Obsidion mark, preview chip) with the body as a SLOT that `PortalPreviewBody` only fills by default, so live code renders as `children` without touching the exhibit treatment, the layout, or the morph target.

**The set piece then shipped.** New: `lib/portal-window.ts` (shared clock, beats, checkpoint band, `seedGeometry`), `PortalStage.tsx` (the pin, 140svh runway, exhibit centred and capped clear of the nav), `PortalExhibit.tsx` (DOM actor + the exhibit treatment on every path). `HomeCanvas` gained the `portal` anchor, approach waypoints, portal stage state, and the cube's dive/flatten/flood beats, and **the services-exit dissolve is retired**: the companion's journey no longer ends in a fade. Built BETTER than the brief specced it: rather than the slab becoming a generic chrome bar, the blue square the cube flattens into IS the Obsidion mark, so the cube dives onto the logo's own measured box and the window stretches and unrolls out from around it. Distinct from the featured-work waterfall by design, and the object ends as the software's identity.

Engineering findings recorded in 9.portal.md: a pin must not hold content that has nothing to show yet (pinning the copy with the exhibit parked clipped text over a void; the exhibit pins alone now); a growing clip always paints something, so the frame needs its own handoff opacity gate; `clip-path` clips shadows, so the frame's shadow rides an outer element; a chrome bar the same colour as the section ground vanishes when it is the only thing on screen (bar moved to `bg-paper`); the flood must finish BEFORE the handoff or the crossfade shows a colour step; and the cube must hold LARGE (0.58, biggest since the hero) or turning in an empty viewport reads as a loading spinner. Probe note: `window.scrollTo` cannot scrub these runways (Lenis lerps it and the checkpoint band glides any programmatic park to its edge), use `page.mouse.wheel` steps.

Verified on the prod build: beats land in order under real wheel input at 1280, the services exit hands the cube over with no seam (it arrives beside the headline), 375/768/1280/1536 + reduced motion all settle correctly (runway 0 at 375 and reduced, 1260px at 768 up), no overflow, zero running animations at rest, zero digits in the window, chip visible everywhere, no page errors, typecheck + build green.

---

## 2F2 Newsletter

2F2 build session (2026-08-25, run in parallel with the portal set-piece session): four decisions taken with Brad up front, all in `9b.newsletter.md`: LIGHT ground rather than the reference's dark band (the page's dark budget is the proof band and the footer, and Portal above is tint); the image panel cycles honest PLACEHOLDER frames until real photography exists; the avatar cluster and client count stay, with his real number, 600+, which this brief is the source of record for; the offer is insights from the leadership team, no cadence claimed. Shipped `Newsletter.tsx`, `NewsletterForm.tsx` (the site's FIRST real input, on the existing `submitForm` path with `formType: "newsletter"`), `NewsletterPanel.tsx`, and `lib/newsletter-frames.ts` (the swap contract: real photos are a data change in one module). Two new STYLE_GUIDE components fall out of it, 6.13 form field and 6.14 cycling media panel, with the light-ground and JS-loop deviations recorded in the changelog. Verified at 375/768/1280/1536 plus reduced motion: no horizontal overflow at any width, the cycle advances 01 to 04 and parks offscreen, reduced motion holds frame 1 with zero running animations, keyboard submit and the announced success state work, typecheck and build green.

Round 2, same session (Brad's two calls after the shots): the section **stays on the homepage** once the closing CTA band ships in 2H, that question is closed. And a newsletter signup **must never navigate to a thank-you page**: the confirmation now animates in where the form was, the wrapper locking to the form's measured height at submit time so nothing below it moves. Verified at 375/1280 plus reduced motion: 0px shift on the proof row, focus lands on the confirmation, the form leaves the DOM entirely (nothing stale in the tab order), reduced motion swaps instantly. The no-navigation rule generalises to every form on the site and is now in STYLE_GUIDE 6.13.

---

## Homepage close restructure (2026-08-25)

**Homepage close restructured 2026-08-25 (Brad, footer session): the page now ends FirstNinetyDays -> closing CTA -> footer.** The 90-days section's ending MORPHS INTO THE CLOSING CTA (built in the parallel 2G1 session; supersedes 10.how-it-works.md v3's no-CTA decision and the standalone 2H band), the footer lands directly under it, and that completes the homepage. Testimonial and FAQ are retired from the homepage (2E-R, 2G, and 2H notes); their patterns and SEO value move to interior pages later. STYLE_GUIDE section 9's closing rows and the page.tsx placeholder note update with the 2G1 and 2I builds.

---

## 2G1 First 90 days

Spec `10.how-it-works.md` v3, added 2026-08-25, supersedes 2G's ProcessCard build; Brad's call after seeing the Youtech frame: the three-card translation was too flat. The 90-square day grid on the returning FeaturedWork blue slab, directly after Newsletter: scroll-driven day clock with pointer scrub and a real slider role, 12 milestone days (INVENTED, placeholder-flagged, `lib/ninety-days.ts` is the one file to fix), merge-to-one-square ending, no CTAs. New: FirstNinetyDays, DayGrid, PhaseReadout, lib/ninety-days.ts.

Round 1 built the no-pin scroll-map version; verified 375/768/1280/1536 + reduced motion, but Brad's review: "very good, not great" (fill too fast, hover scrub chaotic, drag-to-90 whited the board, blank-square ending, weak right column). Round 2 (spec v3.1): pinned 160svh runway scrubbed 1:1 (~1svh/day, sticky offset max(88px,4svh) clears the nav; found by screenshot at 4svh); hover now highlights only and press-drag scrubs with a distance-scaled chasing tween (release eases back over 450ms); the merge is SCROLL-GATED on its own pin beat (never the day value); and the merged square GROWS INTO THE CLOSING CTA via a measured clip-path (white panel in a blue ring, its own white boiling box squiggle, hand-drawn circle on "day 90", Schedule a Call pill with the accent-scope inversion forced off, new RoughAnnotation smiley variant with rearm retract). Readout rebuilt: phase rail chips, display-scale mono day numeral over /90, phase name in the display face. Checkpoint band (bands resolver) covers merge-to-finale at ~1600ms/vh; mobile keeps no pin (stage taller than viewport) and latches the finale once the SCROLL clock completes. Reduced-motion trap found by probe and now binding: framer never fires `animate` on motion elements mounted before useReducedMotionSafe flips, so reduced branches render plain elements. Verified round 2: pinned fill/merge/expand/finale states at 1280, finale at 1536, mobile latch at 375, hover-does-not-move-clock probe (49->49), keyboard slider probe (Home/arrows/PageUp/End/blur-handback), reduced motion 0 running animations, 0 overflow all widths. Awaiting Brad's round-2 review before checkoff.

---

## 2I Footer

Direction chosen from a three-concept brainstorm (Back Cover / 3x3 letter Monument / physics Toybox): THE BACK COVER, the editorial magazine close, tactile-composed, with the paint-the-footer pixel grid as its one fun surprise. Mounts in the marketing layout, so it now appears on every marketing route (funnels stay outside it); the homepage's placeholder band is gone.

2I round 1 REJECTED live by Brad: "weird grid lines" that exist on no other section, too much info, chaotic and sloppy, nearly a full viewport, fonts that do not match. Root cause worth keeping: the v2 brief was written BEFORE the region pivot and specced the instrument layer (info bar, ruled mono link tables, GridLines rails, mono colophon) onto a page whose whole lower region is now the open layout. His references, sent live: Youtech's footer for the simplicity and information layout, e2vc's for the playfulness, minus e2vc's size. Round 2 rebuilt to that cross and the brief plus STYLE_GUIDE 6.8 were rewritten to match (changelog entry "the footer: quiet information, one big gesture").

What shipped: plain link columns (no rules, no arrows, no mono, no rails) carrying the four-column services IA; locations and contact flowing in the SAME grid, each city a 7px brand square with its live local time; the legal line; then the set piece, the logo mark at cap height leading BIGSQUARE in Bluu, sized together to the full viewport width and cropped by the page's bottom edge, glyphs rising into the crop on the footer's own scroll progress and letters flooding accent on hover (the mark inside the crop row is the large-logo half of Brad's ask, and it costs zero height); plus the footer paint layer, the first shipped instance of STYLE_GUIDE 7.6 (desktop fine pointers only). New: `Footer.tsx`, `OfficeClocks.tsx`, `FooterWordmark.tsx`, `FooterPixelGrid.tsx`, `lib/footer-links.ts`. Verified on the prod build at 375/768/1280/1536 plus reduced motion: no horizontal overflow anywhere, footer 77vh at 1280 (73/99/130 at 1536/768/375), 0px between the wordmark crop and the document end at every width, clocks live in both zones with no hydration mismatch, reduced motion settled with zero pixel cells and zero running animations, typecheck and build green.

Decisions taken in the build, all recorded in the brief: **no placeholder strings in site chrome** (socials, office phones, and any partner badge are OMITTED until the facts exist, rather than shipping visible `[PLACEHOLDER]` text on every page); Privacy and Terms live only in the legal line, not also in the Company column; the small logo left the legal line because the giant wordmark is the mark.
