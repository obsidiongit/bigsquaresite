# Handoffs, 2026-08-30: three panes in parallel

Brad runs these three sessions at the same time in this repo. Each pane owns ONLY the files listed under it. Do not touch another pane's files; if you need a change there, write it as a note at the bottom of this file under "Cross-pane requests" and keep going. Re-read a shared file right before you edit it (another pane may have moved it) and never `git add -A`; stage your own paths by name.

Shared rules for all three panes:
- Read first: CLAUDE.md, project-guidelines/project-brief.md, copy-rules.md, STYLE_GUIDE.md, tasks.md ("Brad's check-in, 2026-08-29" is the current state), decisions.md.
- Fonts changed 2026-08-30: Lenia Mono is the ONE family (headlines, body, UI, eyebrows); Casual Human is the handwriting accent (`font-accent`, caps only, one word per moment). Display headlines carry the filled-O alternate (`ss01`) by default; `.alt-o`, `.alt-full`, `.alt-none` are the dials. Use alternates lightly.
- Sound: hover pops are opt-in. Add `data-sfx=""` only to a surface that earns a pop (a primary CTA, a big card). Clicks sound everywhere already.
- Positioning: BigSquare is a full-stack marketing agency with strong creative. Franchise and multi-location is one lane, not the whole road. Ecommerce, software, and single-location clients read as equals. Do not write franchise-only copy on anything new. (The sitewide copy pass on OLD pages is a later conversation; do not start it.)
- No em dashes in user-facing text. No invented numbers, clients, reviews, or awards. Wrap anything unsure in `[PLACEHOLDER: what is needed]`.
- No "+" registration marks on new pages. Open layout (D1). Hero first on every page.
- Before you call a page done: screenshots at 375 / 768 / 1280 / 1536 plus reduced motion, zero horizontal overflow, JSON-LD probe where relevant, `npx tsc --noEmit`. Stop any dev server before `npm run build`. The visual-check recipe: a Playwright script in your session scratchpad, chrome channel, `page.mouse.wheel` for scrolling (Lenis owns scroll; `scrollTo` gets fought).
- Update `sitemap.md` STATUS rows and `tasks.md` checkboxes for what you shipped. End with a short plain list of what you built. Brad reads at a glance; keep the wrap-up to a few lines.
- Commit your own files with a clear message when a piece is verified. Do not push; Brad pushes.

---

## Pane A: blog pipeline, resources, 404 page

You own: `app/(marketing)/blog/**`, `app/(marketing)/resources/**`, `app/not-found.tsx`, `content/**`, `lib/blog.ts`, `lib/resources.ts`, `mdx-components.tsx`, `next.config.ts` (MDX wiring only), `app/sitemap.ts` (add blog + resources routes only), `project-sections/blog/**`.

Goal: the site launches WITH a blog and a resources page, and a scheduled Claude cloud agent will write future posts with zero code changes. Build the blog so that dropping one `.mdx` file into `content/blog/` is the whole publishing step.

1. MDX pipeline. `@next/mdx`, `@mdx-js/loader`, `@mdx-js/react` are already installed (package.json); `next.config.ts` has no MDX wiring yet. Read `node_modules/next/dist/docs/` for this Next version's MDX guide before writing config; this Next is newer than your training data. Add `mdx-components.tsx` at the repo root mapping headings, paragraphs, lists, links, blockquotes, and code to the site's type scale and palette utilities (`text-h2`, `text-body`, `text-ink`, etc., from STYLE_GUIDE 3.2). Long-form copy sits on the ~65ch content spine (STYLE_GUIDE 4.5).
2. Content model. `content/blog/<slug>.mdx` with frontmatter: `title`, `description`, `date` (ISO), `author` ("BigSquare Team" for now), `tags` (2 to 4), `draft` (boolean). `lib/blog.ts` reads the folder at build time, parses frontmatter, sorts by date, and hides `draft: true` posts everywhere (index, sitemap, static params). Reading time is computed, not stored.
3. Pages. `/blog/` index: a hero (statement headline, one line of what the blog is for), then an editorial list, not a card grid: date, tag, title as a big link, one-line description, ruled rows (the RuledLinkTable / NumberedRuledList family in components/shared). `/blog/[slug]/`: title, date, reading time, tags, the body on the spine, an author line, a "more posts" strip of 2 to 3, and one CtaBand to /audit/. Article JSON-LD on posts (author Organization: BigSquare Marketing; `SITE_URL` from lib/site). Both routes join `app/sitemap.ts`.
4. Two launch posts. Real, useful, 900 to 1400 words each, following copy-rules.md, on topics that fit the wide positioning (one that is not franchise-specific). Suggested: "How to tell if your marketing agency is actually working: 7 numbers to ask for" and "Local SEO for brands with more than one location: what changes at scale". Link 2 to 3 internal pages each. No invented statistics; where a stat would help, write the sentence without one.
5. `content/blog/TOPICS.md`: a queue of ~10 future post ideas. One line each: working title, target keyword, angle in ten words, which internal page it should link to. Mix the lanes: ecommerce, software, home services, legal, healthcare, franchise. This is what the scheduled writer reads; `project-sections/blog/routine-prompt.md` is its prompt. Make sure the prompt still matches what you built (frontmatter names, folder, TOPICS format); edit the prompt if you changed the contract.
6. `/resources/`. A real page, not a shell: hero, then a ruled list of the 5 lead magnets from `project-sections/lead-magnets/` (title, one line, format, "Get it" link). The assets do not exist yet, so each link goes to a `[PLACEHOLDER]` form state on the same page (name + email through the shared `submitForm` path and `Field` primitive, in-place confirmation) rather than a download. `/resources/[slug]/` stays 404 until assets land. Add `/resources/` to the sitemap.
7. `app/not-found.tsx`. Brad: "I want the 404 page to be fun and unique, I love when brands do that. Big font, portfolio / editorial style." Make it a page people screenshot. Direction: the whole viewport is one giant piece of display type (the `text-display` scale or bigger, Lenia Mono 700, the filled-O alternate is your friend here since "404" and "NOT FOUND" are full of O's), one clever line under it, and two links (home, /contact/). Use the brand square somewhere: the cube is not available here (HomeCanvas is homepage-only, too heavy), but a 2D blue square that reacts to the pointer or the scroll is cheap. Consider a moving element that never repeats (a random line from a short list of 5 or 6 jokes that stay in BigSquare's voice, rendered client-side so the page still statically renders). It must still work with reduced motion and at 375. Keep it under 30 lines of copy total. One accent word in `font-accent` is allowed. No nav changes; the shared Nav and Footer render as usual. Add `metadata` with a title, `robots: noindex`.
8. Verify all of it at the four widths + reduced motion, `tsc`, then `npm run build` with the dev server stopped. Update sitemap.md rows: `/blog/`, `/blog/[slug]/`, `/resources/`, plus a new row for the 404. Update tasks.md (Batch 4 line and the Pane A line).

Wrap up with the list of files you built and the two post titles.

## Pane B: funnel templates (/go/, /apply/, /thanks/) and the ad-credit slot

You own: `app/(marketing)/go/**`, `app/(marketing)/apply/**`, `app/(marketing)/thanks/**`, `lib/funnels/**`, `components/sections/funnels/**`, `project-sections/landing-pages/**`, `app/(marketing)/ad-credit/**` (only if Brad's terms arrive; otherwise do not create it).

Goal: Batch 3. Three template types, one green-lit flagship each, built inside this batch. These are ad destinations: no nav, no footer, `noindex`, no links in the sitemap. Alternate palettes are allowed on these pages only, one palette per page (D6): the `[data-theme]` grounds in globals.css (light, tint, dark, accent).

1. Read the two specs: `project-sections/landing-pages/vsl-template.md` and `application-funnel-template.md`. They predate the design pivots; the SECTION ORDER and TRACKING requirements hold, the "Graphite/Signal" palette names do not (use the data-theme grounds), and the calendar embed is RETIRED (decisions.md): the booking step is the /schedule/ application form flow, so the VSL page's button routes to /schedule/ with UTM carried, and the application funnel posts through the shared `submitForm` path.
2. Layout. These routes need a layout that does NOT render the marketing Nav, Footer, or the sound toggle. Check how `app/(marketing)/layout.tsx` mounts them; the cleanest answer is a sibling route group (`app/(funnel)/go/[slug]/`, etc.) with its own minimal layout that still mounts `SmoothScroll`, `PageTransitions`, `SoundProvider`, and `UtmCapture` if those live in the marketing layout. Confirm the URLs stay `/go/[slug]/`, `/apply/[slug]/`, `/thanks/[slug]/` with trailing slashes. Update `app/sitemap.ts` NEVER to include them (it already excludes them; keep it so). `robots: { index: false, follow: false }` in metadata.
3. Content registry. `lib/funnels/registry.ts`: a typed map of slugs to funnel content (headline, sub, video URL + poster, proof bullets, steps, fine print, palette). One flagship slug each: `/go/audit/` (VSL that sells the free audit), `/apply/growth-partner/` (the qualifying application), `/thanks/audit/` and `/thanks/growth-partner/` (each thank-you says exactly what happens next and carries the UTMs forward). Unknown slugs 404. All copy is real draft copy in the wide positioning (not franchise-only), placeholders flagged where facts are missing (video URL, proof).
4. VSL page. Logo mark centered, the promise headline (display scale), the sub, a 16:9 custom-styled video slot (a `<video>` with poster and a big play button for now; `[PLACEHOLDER: video URL]` in the registry; no third-party player yet), the CTA pill, three proof bullets or one case-study card (`project-sections/shared/case-study-card.md`), a 3-step "what happens on the call" block, second CTA, fine print + privacy link. Max width ~900px. Fire `video_play`, `video_50`, `video_complete` events through whatever tracking helper exists (grep `lib/` for gtag / pixel helpers; if none, add a small `lib/track.ts` that no-ops until the env IDs exist).
5. Application funnel. Multi-step form, one question per step, progress bar, keyboard friendly, the questions from the spec but with the location-count step reworded so single-location and ecommerce businesses do not feel excluded (add "Online only" to the industry step). Posts through `submitForm` with UTMs and the page slug, then routes to `/thanks/growth-partner/`. Reduced motion: no step slide, just swap.
6. Thank-you pages. Big type, one job: tell them the next step (check email, expect a call within X, `[PLACEHOLDER: turnaround]`), one secondary link to /results/. Keep the UTM params on the URL for the tracking tags.
7. `/ad-credit/`: only if Brad has posted the offer terms in `project-sections/shared/popup-ad-credit.md` or tasks.md. If not, leave it 404 and say so.
8. Verify at four widths + reduced motion, `tsc`, `npm run build`. Update sitemap.md STATUS for the three paid-traffic rows and tasks.md Batch 3.

Wrap up with the four URLs and what is placeholder.

## Pane C: nav rebuild and footer redo

You own: `components/shared/Nav.tsx`, `components/sound/SoundToggle.tsx`, `components/shared/Footer.tsx`, `components/shared/FooterPixelGrid.tsx`, `components/shared/FooterWordmark.tsx`, `components/shared/Pill.tsx` (nav-size variants only), the nav and footer CSS in `app/globals.css` (the `.pill*` block and anything you add; do not touch the font or type-scale blocks), `project-guidelines/STYLE_GUIDE.md` sections 6.1 (pills), 7.x nav/footer entries, plus a changelog entry.

Goal: Brad, 2026-08-29: "I need to revisit the nav bar and the buttons on there. The Menu button I'm not a fan of, the Let's Talk button I'm not a fan of, the Sound button I'm not a fan of. I want to simplify those and make them a bit more unique." And the footer: "needs a much more cleaned up and stylistic approach. It's good, not great. A little too much chaos going on there."

Brad has his own ideas for the nav. ASK HIM FIRST, before building: open the session by showing him a screenshot of the current bar and asking three short questions (one message, plain language): what he pictures for the menu trigger, whether "Let's Talk" stays a pill or becomes something else (a text link, a square, a single icon), and whether the sound control should be a tiny mark in the corner rather than a labeled button. Then build his answer, not a guess. If he says "just do it", the default direction below is the one to build.

Default direction if Brad gives no steer:
1. Nav bar reduced to three things: the logo lockup on the left, one primary action on the right, and the menu trigger. The sound control leaves the button row and becomes a small mark (the three level bars alone, in the brand square footprint, no label, tooltip on hover) that sits at the far right edge or bottom-left corner next to the page-transition mark.
2. Menu trigger: not a pill. A single brand square that turns 45 degrees on hover and becomes the close X when the overlay is open (the square is the brand mark; the trigger is the mark). Label "Menu" in Lenia Mono only at md+, sitting to the left of the square.
3. Primary action: keep the label from the approved list (copy-rules.md; "Let's Talk" is under review, "Schedule a Call" is the sitemap's label) but as a single confident pill in the accent, with the hover sound (`data-sfx`). One pill on the bar, not two.
4. Overlay: keep the big-type menu; it is approved. Tune only what the trigger change requires.
5. Footer: one calm composition. Cut the pixel-grid "paint layer" (Brad flagged keep-or-cut; cut it) and the chaos: row 1 the giant wordmark (Lenia Mono 700, filled O, viewport-scale, the one big move), row 2 the link columns as ruled lists (Company / Organic / Paid / Design & Dev), row 3 the offices with their clocks, socials, support email, and the legal line. Generous space, one hairline per row, nothing boxed. Mobile: columns stack to an accordion or a two-column ruled list, no giant wordmark overflow (clamp it). Footer facts are still placeholders (phones, socials); keep the `[PLACEHOLDER]` tags visible.
6. Sound: the nav pills and trigger keep `data-sfx`; the footer links stay silent on hover.
7. Verify at four widths + reduced motion + keyboard (Tab through the bar, Escape closes the overlay), `tsc`. The nav renders on every page; check /, /services/seo/, /contact/, /schedule/ at 375 and 1280 after the change.
8. Update STYLE_GUIDE (6.1 pills, the nav and footer entries) and tasks.md ("Footer facts" and the Pane C line).

Wrap up with before/after screenshots of the bar and the footer at 1280 and 375.

---

## Cross-pane requests

(append here; owner picks them up)
