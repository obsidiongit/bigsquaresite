# Handoffs, 2026-08-31: launch polish, two panes

State on 2026-08-30 (end of the three-pane session): all 3 template families are built and every URL in sitemap.md resolves except the ones blocked on Brad's content. The funnel templates are done for good; a new campaign page is one entry in `lib/funnels/registry.ts`, no code. What is left is polish and content, not new templates.

Same shared rules as `handoffs-2026-08-30.md`: read CLAUDE.md, project-brief.md, copy-rules.md, STYLE_GUIDE.md, tasks.md, decisions.md first. Lenia Mono is the one family, Casual Human is the caps-only accent (one word per moment). Hover sfx opt-in via `data-sfx=""`. No em dashes, no invented numbers or clients, `[PLACEHOLDER: what is needed]` for anything unsure. No "+" registration marks on new work. Each pane owns only its files; cross-pane requests go at the bottom of this file. Stage your own paths by name, never `git add -A`. Do not push; Brad pushes. Screenshots at 375 / 768 / 1280 / 1536 + reduced motion, zero horizontal overflow, `npx tsc --noEmit`, `npm run build` before calling anything done. One dev server per repo (Next 16 refuses a second one in the same directory): if another pane holds :3000, use it; it compiles your routes on demand.

Pending Brad reviews (say which ones you are picking up fixes for, if any): Batch 2 (/locations/, /results/, /careers/), Batch 3 (/go/audit/, /apply/growth-partner/, /thanks/*), Batch 4 (/blog/, /resources/, the 404), the nav + footer rebuild.

---

## Pane A: the sitewide copy pass

You own: copy inside `app/(marketing)/**` pages, `lib/service-pages/**`, `lib/industry-pages/**`, `lib/location-pages.ts`, `lib/faq.ts`, `lib/footer-links.ts`, `components/sections/**` TEXT ONLY (no layout or motion changes), `app/layout.tsx` metadata, `components/shared/OrganizationJsonLd.tsx`, `project-guidelines/project-brief.md` (positioning amendment), `project-guidelines/copy-rules.md` (one added rule).

Goal (Brad, 2026-08-29): "BigSquare is a full-stack marketing agency with strong creative. Franchise and multi-location is one big lane, not the whole road. Ecommerce, software brands, and single-location clients read as equals." ~210 franchise/multi-location mentions across ~38 files. Titles, metas, heros, JSON-LD, and the home title tag all change.

1. Inventory first. `grep -rn -i "franchise\|multi-location\|multi location" app lib components --include=*.ts --include=*.tsx` into a scratchpad list, grouped by file. Post the count before you start.
2. Rule of thumb per mention: KEEP it where the page is about franchise or multi-location (the /industries/franchise/ page, the D4 wave-2 service slugs, location-count pickers, the 3-of-6 case-study tags that are really about it). REWRITE it where it is the default audience (home hero, service page heros and metas, the root title tag, the org description, CTA bands, FAQ answers, footer). Keep the reading level (Flesch 70+), keep the voice, keep the approved button labels.
3. Root title tag: `Marketing Agency for Brands That Want Proof | BigSquare` is a draft; write 3 options in the wrap-up for Brad and ship the one you would pick. Same for the home meta description.
4. JSON-LD: the Organization description and every service/industry/location page description follows the same rule. Probe each changed page's JSON-LD after.
5. Add one rule to copy-rules.md under "Claims" or a new "Audience" heading: the wide-positioning sentence above, verbatim, dated. Amend project-brief.md "Positioning in one line" and "Who is it for" to match (keep the franchise paragraph, add the others).
6. Verify: every touched page at 375 and 1280 (headline length changes break layouts; a longer H1 can wrap to a 4th line and push the hero), `tsc`, build. Update sitemap.md title-tag drafts for every page whose title changed.

Wrap up with: the mention count before and after, the list of files touched, the 3 title-tag options.

## Pane B: launch readiness (2J) and review fixes

You own: `app/layout.tsx` (metadata + icons only), `app/opengraph-image.tsx`, `app/icon.*` / `app/apple-icon.*` (new), `public/` icon files, `next.config.ts` (headers/images only), `lib/site.ts`, the Batch 3 files (`app/(funnel)/**`, `lib/funnels/**`, `components/sections/funnels/**`) for any review fixes, and `project-guidelines/asset-manifest.md`.

1. Favicon + app icons: the favicon 404s today. Build `app/icon.svg` (the brand square in --acc, the logo mark from `assets/logo.svg`) plus `apple-icon.png` (180px) with the file-based metadata API (read `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/app-icons.md` first; this Next is newer than your training data). Confirm no 404 in the console on /.
2. OG image: `app/opengraph-image.tsx` exists; check it renders the Lenia Mono lockup (it may still load Bluu/Apfel) and that every page inherits it. Per-page OG titles come from metadata; no per-page images yet.
3. Metadata sweep: every route has a title, description, and canonical (`alternates.canonical`); funnel routes stay noindex. Write a scratchpad script that curls every URL from `app/sitemap.ts` plus the funnel routes and prints title / description / canonical / robots; fix gaps.
4. Lighthouse: run it (Playwright or the CLI in the scratchpad, never added to package.json) on /, /services/seo/, /schedule/, /go/audit/, /blog/ at mobile. Target CWV green. Likely suspects: the hero video preload, the font preload list in `app/layout.tsx` (still preloads Bluu/Apfel, which are fallbacks now; preload Lenia Mono 400/700 instead), the three.js bundle on / (known ~232KB gz overage, Brad's call; report, do not rip out). Report scores before and after in the wrap-up.
5. Review fixes: if Brad has posted notes on Batch 3 (the funnels) by the time you start, apply them here. `/ad-credit/` is RETIRED (Brad, 2026-08-31): do not build it, and remove any stray reference you meet.
6. Content drops: as Brad's designer delivers files, sweep `asset-manifest.md` slots into `public/media/` + `lib/asset-files.ts` rows. Only what has actually landed.

Wrap up with: the icon files, the metadata gaps you fixed, Lighthouse before/after per page.

---

## Blocked on Brad (no pane can build these)

- `/leadership/`: names, titles, photos
- `/resources/[slug]/`: Brad picks 5 from `project-sections/lead-magnets/brainstorm-2026-08-31.md`, then the files
- `/results/[slug]/`: real case studies (`lib/featured-work.ts`)
- `/privacy-policy/`, `/terms/`: the facts listed in `project-sections/legal/legal-pages-plan.md` (Claude drafts, lawyer reviews)
- Funnels: the VSL video URL + poster, one sourced result, budget ranges + qualifying floor, call turnaround
- Env: `FORM_WEBHOOK_URL`, `NEXT_PUBLIC_META_PIXEL_ID`, `NEXT_PUBLIC_GTAG_ID`, `NEXT_PUBLIC_GA4_ID`
- Music loop (`MUSIC_SRC`), the office phones/addresses, socials, the logo file

## Pane C: content-blocked pages, laid out now (added 2026-08-31)

You own: `app/(marketing)/leadership/**` (new), `lib/leadership.ts` (new), `app/(marketing)/results/[slug]/page.tsx`, `lib/featured-work.ts` (shape only; keep the 6 entries), `app/(marketing)/privacy-policy/**`, `app/(marketing)/terms/**`, `content/legal/**` (new), `components/sections/legal/**` (new), `app/(marketing)/resources/[slug]/**` (new), `lib/resources.ts` (shape only), the consent line inside the shared form components (`components/shared/Field.tsx` area, coordinate with Pane A if they are mid-copy-pass in a form file: post in Cross-pane requests first), `components/sections/funnels/ApplyForm.tsx` (consent checkbox only), `lib/form-action.ts` (consent fields only), `lib/footer-links.ts` (one link), `project-sections/legal/**`, `project-sections/lead-magnets/**`, `project-sections/results/**`, `project-sections/company/leadership.md`.

Brad, 2026-08-31: the pages waiting on his content still get built as far as they can go, so the content drop later is data, not code. Ad credit is dead; do not build it.

1. `/leadership/`. Read `project-sections/company/leadership.md` (it predates the pivots: "Apfel Fett" is now Lenia Mono 700, the open layout applies, no registration marks). Build `lib/leadership.ts` (name, role, bio, photo slot, linkedin) with 3 entries whose every field is a visible `[PLACEHOLDER]`; hero (eyebrow, H1 "The people running your account", one intro line flagged), a 3-up card grid (square photo slot via MediaSlot, name, role as eyebrow, bio), CtaBand. `robots: { index: false }` and NOT in `app/sitemap.ts` until real people land (leave a one-line comment in sitemap.ts saying so). Add the nav/footer link only if the nav already lists Leadership; otherwise leave nav alone (Pane C of 08-30 owns it).
2. `/results/[slug]/`. Make the template data-driven. Extend `WorkEntry` with an optional `caseStudy` object: `headline` (the result sentence), `summary`, `metrics` (exactly 3: `value`, `label`, `window`, `source`), `situation` (paragraphs), `steps` (3 to 5, each `text` + `serviceSlug`), `result` (paragraphs), `quote` (`text`, `name`, `role`, optional; real names only), `services` (slugs). The page renders the placeholder skeleton when `caseStudy` is absent and the real layout when present; `robots.index` is true only when present. Title tag follows the "[Result] for [Client type] | BigSquare" pattern from `project-sections/results/_case-study-template.md`. Add BreadcrumbList JSON-LD. Fill NO entry with invented data; the 6 stay skeletons.
3. Legal pages. Read `project-sections/legal/legal-pages-plan.md` in full. Build `content/legal/privacy-policy.mdx` and `content/legal/terms.mdx` with frontmatter (`title`, `effectiveDate`) and the H2 clause list from the plan as headings, Brad delivered ALL facts on 2026-08-30 (see "Brad's answers" + "Final calls" in that file: LLC, Florida law + arbitration, retention defaults approved, NO address anywhere by his decision, email-only contact, never name the CRM vendor). So: draft the FULL plain-English text for both pages from the clause lists. No invented tools or addresses; name only what the plan lists. Say clearly in your wrap-up that a lawyer still needs to read it. Render both pages through the blog's MDX pipeline on the 65ch spine: H1, "Effective <date>" line, a sticky "On this page" H2 list at lg+, the body, a contact line. Keep canonicals; these pages ARE indexed and ARE in the sitemap already.
4. Consent. Under every form's submit button: "By sending this you agree to our [Terms] and [Privacy Policy]." (one shared line in the form primitive). On `/apply/` (phone field): an unchecked checkbox with one-to-one SMS consent text naming BigSquare Marketing, message frequency, "message and data rates may apply," STOP/HELP, and links to both pages; submission records the consent text, a timestamp, and the checkbox state through `submitForm` (add the fields to `lib/form-action.ts`). Do not gate the form on the SMS box; gate only the texting.
5. Footer: add "Do not sell or share my personal information" linking to `/privacy-policy/#your-choices-and-rights` (the H2 id the MDX pipeline generates; confirm the slug it produces).
6. TABLED (Brad, 2026-08-30): do NOT build `/resources/[slug]/` or touch `lib/resources.ts`. Lead magnets are parked until Brad picks them (before launch, tracked in tasks.md). Skip this item. (Original scope, for when it un-parks: build the lead-magnet page template per `project-sections/lead-magnets/_lead-magnet-template.md` (hero with form, cover mockup slot, what is inside, who it is for, proof slot, second form) driven by `lib/resources.ts` (add `promise`, `inside[]`, `cover`, `file` fields; all placeholder). `generateStaticParams` from the 5 rows; `robots.index` false and out of the sitemap until a row has a real `file`. The /resources/ index keeps its on-page request forms.)
7. Verify all at four widths + reduced motion, JSON-LD probe on a results page, `tsc`, `npm run build`. Update sitemap.md STATUS rows and the "Content-blocked pages" checklist in tasks.md.

Wrap up with the list of routes, what is placeholder, and the exact facts still owed by Brad for the legal pages.

## Cross-pane requests

(append here; owner picks them up)

- **Pane C -> Pane A (notice, 2026-08-31):** adding the legal consent line (new `components/shared/ConsentLine.tsx`) directly under the submit button in `ContactForm.tsx`, `AuditForm.tsx`, `ScheduleForm.tsx`, `ApplyForm.tsx` (plus the SMS checkbox + consent plumbing there), and `resources/ResourceList.tsx`. Layout-only additions at the bottom of each form; no existing copy touched. If you are mid-pass in one of these files, re-read before saving.
- **Pane C -> Pane A:** `components/sections/home/NewsletterForm.tsx` is homepage-owned, so I did not touch it. It also needs `<ConsentLine />` under its submit button (one import + one line).
- **Pane C -> Pane B (superseded same day):** /leadership/ became /team/ on Brad's review; the `leadership-01..03` slots are DEAD, never add them. The team page reuses the existing `blog-author-*` headshot slots and adds 18 personal photo slots `team-<first>-1..3` (first in brad, mike, chaley, levi, russel, sadie; square 1:1, each member's own photos). Add those rows to `asset-manifest.md` instead.
- **Pane C -> whoever wires tracking:** the shipped privacy policy now states we honor Global Privacy Control. Before the ad tag IDs go live, the tag loader must skip the ad tags when `navigator.globalPrivacyControl` is true (legal-pages-plan.md build step 4). Today no ad tags load (env IDs empty), so the statement holds.
