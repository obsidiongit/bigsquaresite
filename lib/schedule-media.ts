/* The /schedule/ VSL (conversion/schedule.md v2): a 1-to-2-minute film
   is the page's trust builder (Brad, 2026-08-26). The asset is not
   delivered yet; this module owns the swap so the real film is a
   one-line change (the decisions.md brand-video contract). While
   VSL_VIDEO is null the player renders the poster, an inert play
   square, and a visible placeholder chip; setting it makes the player
   live, no layout change.

   The poster reuses the dark abstract placeholder loop's frame until
   the real film ships with its own poster.

   [PLACEHOLDER: VSL film + its poster frame] */
export const VSL_VIDEO: string | null = null;
export const VSL_POSTER = "/media/hero-poster.jpg";
