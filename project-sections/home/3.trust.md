# Trust Marquee Design Brief

v2.6, 2026-08-24 (card sweep session, Brad's ask). Two additions. (1) The eyebrow "Some of the partners we work with" wears the HERO-CIRCLE treatment: `RoughAnnotation variant="circle"`, drawing on entry and boiling in place, kept clear of the marquee row (his constraint). His round-2 fix applied: the bare text box read "too tight... clipping the words" (the flat ellipse crossed the line ends), so the annotation wraps a px-4/py-2.5 padded box, and the boil runs calmer via the new `boilMs` prop (320ms vs the 200 default, too jittery at 13px type). Budget exception, his ask. (2) The companion cube now RE-EMERGES here: it travels hidden behind the Search + ProofBand occluders (z-[6] grounds) and slides out from under the proof band's bottom edge over this strip (7.proof-numbers.md v3.4).

v2.5, 2026-08-24 (Brad's full SVG drop). Brad supplied 16 more SVGs and directed: use them all, drop any text-wordmark placeholders left without a file. The strip is now 24 logos, all SVG, no text fallbacks (the fallback code path remains but is unused; a new partner needs its SVG to enter). The list grew past ad platforms into the tools we build and operate with (his curation, superseding the v2 confirmed-launch-set lock): Meta, Google Ads, TikTok, Microsoft (four-square mark replaces the "Microsoft Advertising" text placeholder), Amazon Ads, Google Analytics, Ahrefs, Yext, Klaviyo, Zoom, Stripe, Google Cloud, Vercel, Atlassian, Webflow, WordPress, Ghostty, Unity, WebGL, Three.js, Anthropic, Claude, Codex, Cursor. Ordered to alternate wordmarks and compact square icons. File hygiene applied on intake: renamed to kebab-case, anthropic and google-cloud viewBoxes cropped to content bounds (getBBox), all files verified to carry a viewBox and light-ground-visible fills (zoom's white glyph sits on its own blue badge).

v2.4, 2026-08-24 (more SVGs + seamless-loop fix). Brad delivered google-ads, yext, and KLAVIYO SVGs (Klaviyo was a candidate; supplying its file confirms it into the strip, now 9 logos; only Microsoft Advertising remains a text wordmark). File hygiene rules learned: SVGs delivered WITHOUT a viewBox (yext, klaviyo had only width/height) must get one added or the img clips instead of scaling; padded canvases get the viewBox cropped to getBBox (amazon). Loop bug fixed (Brad's catch: logos after WordPress vanished on the right until the wrap caught up): the track rendered exactly 2 copies of the set, which runs dry on wide viewports; it now renders 1 + ceil(viewport / set width) copies (min 2, re-measured on resize and as SVGs load) and wraps the offset on ONE set's measured width.

v2.3, 2026-08-24 (open strip, Brad's live notes). The bordered gray tiles are DROPPED and logos render FULL COLOR at all times (no grayscale filter, no grayscale-to-color hover): an open floating strip like Youtech's, matching the de-railed region (the v2.1 "possible future option" is now the direction). First five SVGs wired (meta, tiktok, amazon-ads, ahrefs, wordpress; Google Ads, Microsoft Advertising, Yext still text wordmarks). The amazon-ads seeklogo file padded its mark inside an 800x600 canvas (mark only ~23% of the height, rendering tiny); its viewBox is cropped in-file to the measured content bounds (104 230 594 148) so it height-normalizes like the rest. Crop any padded future file the same way (measure with getBBox) rather than per-logo height overrides; the `imgClassName` escape hatch exists but is unused. STYLE_GUIDE 6.4 (logo item) + 7.5 updated with a changelog entry. Same session, two more Brad notes: the HOVER PAUSE is retired (the strip never stops) and the velocity coupling now uses the scroll speed MAGNITUDE clamped to +3x base, so scrolling up speeds the strip forward instead of reversing it.

v2.2, 2026-08-24 (position update again, supersedes v2.1). In Brad's proof-band session the marquee moved from the region bottom to **directly under the dark proof band**, which itself now sits directly under Search: band + marquee form one extended trust block (Youtech frame 05, stats band with the partner logos beneath). Component unchanged. Real logo SVGs get dropped in `public/logos/` (README inside documents naming); wire each via the `src` field on its `LOGOS` entry.

v2.1, 2026-08-24 (position update only). In the region pivot locked with Brad, the marquee **moves to the bottom of the lower homepage region**: after the dark proof band, before the unbuilt phases (matching Youtech's partner-strip placement under its stats band). The component built in 2C is reused as-is; only its slot in `page.tsx` changes. Possible future option, noted not planned: dropping the bordered tiles for a more open strip to match the de-railed region. Everything below is the v2 brief, still accurate for the component itself.

v2, 2026-08-23. Replaces the pre-scrape spec's bare floating-logo marquee with the bordered tile treatment. Built in phase 2C. STYLE_GUIDE.md 6.4 (logo tile) and 7.5 (marquee rules) govern.

Role: quiet cataloged proof. Tiles, not floating logos: it should read as an inventory, not a sticker strip.

## Content

### Label (centered, eyebrow style)
"Some of the partners we work with"

### Logo marquee
Tools and platforms we actively run campaigns on or build with.

Confirmed for launch: Meta, Google Ads, Microsoft Advertising, TikTok, Amazon Ads, Ahrefs, Yext, WordPress
`[PLACEHOLDER: confirm the rest of the list. Candidates: Google Analytics, Klaviyo, CallRail, Shopify, Webflow, Vercel, Figma]`

### Target state (do not build yet)
When earned, a second row carries the official partner badges (Google Partner, Meta Business Partner, Microsoft Advertising Partner). Build so a `badges` array can be added without redesign. Future-state reference: `../reference-images/scorpion-partner-badges.png`. Never imply partner status we do not have: the label says "work with", not "certified by".

## Design direction

- Light section, compact: 48px vertical padding, sits under the hero info bar. No Nº label (utility section).
- Each logo lives in a **tile**: `--surf` background, 1px `--line` border, radius 16, logo grayscale and height-normalized to 32px inside comfortable padding (tile roughly 150x80). Full color on hover, 250ms.
- One marquee row (8 to 15 logos does not justify two offset rows). Track duplicated for the seamless loop, duplicate `aria-hidden`.
- Soft fade masks at both edges.

## Steal from

- Metacci, grayscale logos inside bordered tiles: `../reference-images/metacci/metacci-home-desktop-03.png`
- Youtech, the partner marquee placement under the stats band: `../reference-images/youtech-agency/youtech-home-desktop-05.png`

## Build with

Existing: `components/shared/Section.tsx`, `Container.tsx`, mono eyebrow from `components/shared/mono.tsx`.

New this phase: `components/sections/home/TrustMarquee.tsx` with `LogoTile`. Logos as SVG in `public/logos/` where possible; a `logos` array drives the strip.

## Motion

- CSS keyframe track at ~50px/s desktop, 35px/s mobile. Pauses on hover.
- Velocity coupling (STYLE_GUIDE 7.5): scroll velocity times ~0.5 added to track speed, clamped to 3x base, decaying 0.9 per frame. This is the page's ONE velocity-reactive element.
- Reduced motion: no marquee; render a static centered wrapped grid of the same tiles.

## Mobile (375)

Same marquee at 35px/s, tiles slightly smaller (logo 28px). Fade masks stay. Reduced-motion grid goes two columns.

## Done when

- [ ] Seamless loop with no visible seam at any width
- [ ] Hover pause + grayscale-to-color work; velocity boost feels subtle, not jumpy
- [ ] Reduced motion shows the static grid
- [ ] Label copy exactly as written (legal-sensitive)
- [ ] Screenshots at all four widths reviewed with Brad before checkoff
