# Featured Work Design Brief

v7, 2026-08-25 (round 9, Brad: the parked cube sat "down too low and too far to the right", and the panel "appears almost instantaneously... too clunky and too rushed"): three changes. (1) The cube now ASCENDS to the float spot INSIDE the hero reform's last beat (R_ASCEND 0.86-0.98 of the reform, desktop only), so the release REST already shows it raised in the headline/support gap; the float spot lifted to (0.14, 0.24), the measured midpoint of that gap at 1536x864 (headline right edge 891px, support left edge 1075px); heroEnd and the pre-pin work waypoint both mirror it. The film card still develops/quenches at the low REFORM_END landing, so the scroll-up story is unchanged. (2) The runway grew to 180svh desktop / 100svh mobile and the ladder re-spaced with the handoff at 0.7, so the top-down panel sweep owns the last ~30% (~50svh) and waterfalls. (3) The checkpoint band now covers only the COMMITTED morph (MORPH_REST 0.3 -> 1, the dive on): the float/spin zone parks freely, and an idle park past the dive completes the morph on a SLOW glide (glideMsPerVh 2600 / max 5000ms, a new per-instance useScrollCheckpoints option), so the auto-completion reads as the animation playing rather than a jump cut.

v6, 2026-08-24 (round 8, Brad: round 7's merged band "just shoots straight there... blasted past the cube spinning animation entirely"): the morph now runs on a PIN RUNWAY: the header + grid stick (sticky top 6svh inside the `[data-work-stage]` wrapper) while a 110svh spacer (70 mobile) of scroll scrubs the whole choreography 1:1 hero-style; the round-7 mega-band and hero checkpoint change are reverted (bands cover only the runway and the exit window); REFORM_END moved back down to (0.2, 0.02) and the cube ASCENDS to the float spot via waypoints between the release and the pin, which also fixes the scroll-up "shrunken media player box on the right" (the film card now develops where the panel balloons from). See "Cube companion" below. v5 (round 7): widened position-driven windows, thickness-ridden reform alpha, mobile DOM-led bar grow (the alpha and mobile pieces survive). v4 (round 6, white inner border clashed, the panel "just appears", no checkpoint): the morph became canvas-led to a geometry-matched handoff, the squiggle became an own-blue edge treatment, and the morph got checkpoint bands; see "Cube companion" below. v3 (round 5, Brad's markup screenshots): the settled cube moved up-left and the cube's grid passage became the WORK PANEL MORPH. v2 (same day): v1 was rejected live by Brad mid-build: it framed the section in rails, hairlines, and mono instrument text inside the 1200px container ("compacted... clunky... too mechanical"), and it sat as a normal section AFTER the hero's empty release viewport. The corrected direction: lusion's work grid at lusion's SCALE (his reference screenshot: giant headline, small uppercase support pinned right, oversized cards, fun portfolio feel), arriving AT the hero's release rest beat, not after it.

Role: the receipts, moved to the front. The hero film says "we make premium things"; this section says "here are 6 of them, go read the numbers." This section supersedes the 3-card grid half of `8.case-studies.md` (note at the top of that file); the 2E proof band is unchanged.

## Placement: inside the release beat

The hero's reform leaves the sticky stage's last viewport as empty paper (and the release checkpoint PARKS there). The section pulls itself up into that band (`-mt-[48svh]` mobile / `-mt-[72svh]` desktop, round 3: Brad flagged the headline landing mid-screen with a big gap above) so the display headline rises from below WHILE the film un-balloons into the cube, and the release rest state reads: headline in the upper third, cube floating on the RIGHT in the band between the support text and the first card row (round 3: at the reform's own end position, CARD_CENTER left, it clashed with the headline and cards; the heroEnd waypoint now sits right-of-center and the follower swims the cube over as the page parks). The overlap depth is bounded above: any deeper and the section's top edge climbs over the settled film panel before the reform starts. The hero's post-wrapper info bar is removed (nothing between the hero and the work).

Round 4 refinements (Brad, same day): the header (headline + support) is scroll-GATED on the canvas path: hidden while the film beats play, a quick load-in (baseline rise + support fade) once the section climbs clear (~46svh desktop / ~61svh mobile header top), re-arming on scroll-back so the text can never sit over the film (a sanctioned two-way exception to fire-once reveals; it exists purely to keep overlapped ink off the set piece). The reform itself now shrinks the panel down to the RIGHT: its shrink target slides from CARD_CENTER to REFORM_END (0.24, 0.03 desktop; HomeCanvas), where the cube forms at its companion settle scale, so there is no left-then-right dogleg and the heroEnd handoff is motionless. The settled panel margin grew to max(6vw, 96px) to clear the nav bar.

The pull-up only applies when the canvas hero runs. Reduced motion and no-WebGL heroes end settled (no empty tail), so the section falls back to normal `pt-section-y` rhythm. Order: Hero, Featured Work, Trust, Problem, Solution, Services.

## What this section does NOT use (Brad's calls, binding)

- No GridLines rails, no SeparatorIn, no registration marks.
- No Nº label and no bracketed indexes: the section is unnumbered (like trust); Problem stays Nº002. The only mono text is the card tag rows and the visible placeholder markers.
- Not the 1200px container: edges at `max(5vw, 40px)` (lusion's base padding).

## Content

- H2: "Featured work" at `--text-display` in Bluu (display-scale section headline; exempt from the flat-H2 rule like the footer wordmark, see STYLE_GUIDE changelog).
- Support, pinned right, max ~38ch, 13px Apfel 500 uppercase (reads as an extended eyebrow): "A selection of work for multi-location and franchise brands. Open any project for the full story and the numbers behind it."
- Grid: 6 cards from `lib/featured-work.ts`, 2 columns desktop / 1 mobile, `gap-x 2vw`, huge vertical gaps (gap-y 144px desktop / 80 mobile). Real clients only; until real case studies exist, titles and media render visible `[PLACEHOLDER]`s. Tags are real service names.
- Under the grid: secondary pill, centered: "See All Results" → /results/.

## Card anatomy (lusion frames `-07.png` to `-13.png`)

1. **Media**: aspect 3:2, radius 24, `--darkpanel` ground. Placeholder composition: oversized Bluu index numeral at 7% `--ondark` bottom-right, mono `[PLACEHOLDER: case film]` bottom-left. Real media swaps in per entry with no layout change.
2. **Tag row**: 12px mono uppercase `--sec-mid`, names dot-separated ("Paid Search • Web Design").
3. **Title**: `--text-menu` scale in Bluu, no hairline. Hover: a `→` at the same scale slides in from the left and the title shifts right ~1.15em (lusion's arrow-leads-title move); media interior scales 1.03 over 600ms.

The whole card is one link to `/results/[slug]/` (skeleton pages, template order, all placeholders, noindex until real data; static params from the data array so no card 404s).

## Cube companion + the work panel morph (v4, Brad rounds 5-6, 2026-08-24)

`work` anchor. The settled cube (WORK_FLOAT, mirrored by the heroEnd waypoint; delivered by the reform's R_ASCEND) sits at (0.14, 0.24): the measured midpoint of the whitespace band between the headline and the support column, raised so its center rides just above the headline's top line at the release rest (round 9), so it never clips behind card 02 as the grid rises and never reads as parked "too low, too far right".

The v2 center-seam journey is replaced by the WORK PANEL MORPH, driven since round 8 by the PIN RUNWAY. Master clock in `lib/work-panel.ts` (workMorph): the header + grid are `position: sticky` (top 6svh) inside the `[data-work-stage]` wrapper whose extra height is a real spacer element (180svh desktop / 100svh mobile since round 9, "spread out these animations"; NEVER wrapper padding: sticky offsets are constrained to the containing block's CONTENT box, and a padding runway silently never sticks: found live). Pin progress is (child.top - wrapper.top) / (wrapper.height - child.height): pure rect math, no configured offsets, settled on fallback paths where the runway renders 0. The panel bottom crossing 0.55vh -> 0.05vh runs the exit at the grid's far edge, position-driven as before. Both actors read the same elements with the same math every frame, no shared state. During the pin the whole release composition (headline, support, first card row peeking) FREEZES while the cube plays its beats against it: the "square spinning animation" Brad wants to actually scroll through. NOTE: the section's `data-cube-anchor` rect keeps moving during the pin (the section is not the sticky element), so waypoint fracs advance mid-pin; the pin-clock blends own the cube there.

The transition is CANVAS-LED to the handoff (round 6: a DOM fade-in read as "the box just appears"): one continuous object, no fade. Runway beats (of pin progress m, round 9 spacing): the cube arrives at the float spot ALREADY (the reform's R_ASCEND); hold blend 0.02-0.2 pins it there against mid-pin waypoint drift, showcase spin 0.02-0.44 (+1.3 rad on top of the idle turn), dive 0.3-0.55 (0.3 == MORPH_REST, the checkpoint band start), flatten 0.44-0.62, flood 0.48-0.66, stretch 0.62-0.7, handoff 0.7, sweep 0.72-1 (~50svh of top-down waterfall), captions 0.78-0.94, border 0.96+.

- **Cube -> bar (canvas)**: the cube dives to the bar's center, unwinds to the nearest HALF turn (a quarter-turn landing shows the cube's side, which the flatten squashes to a sliver), flattens, floods to `--acc` on the EMISSIVE channel (base color to black, env light to zero: a lit PBR base color can never match the CSS hex), then STRETCHES into the bar: full panel width x PANEL_BAR_VH (0.14vh), the panel's top slice on enter / bottom slice on exit. Follower damping ramps 4 -> 28/s approaching the handoff so lag can't offset the swap.
- **The handoff (PANEL_HANDOFF, morph 0.8)**: the DOM panel opacity-swaps over the slab's identical same-color geometry, then sweeps scaleY down behind the cards (origin top on enter, bottom on exit). The slab hides just after; while hidden its follower stays pinned to the live target so the reverse morph starts clean.
- **The panel (DOM)**: ONE brand-blue box (`--acc` via a data-theme="accent" scope, radius 32) behind all six cards, extending max(2.5vw, 20px) past the card edges, clamp(64px, 10vh, 128px) above the grid (headroom the bar stretch plays in; also the settled panel's header band) and clamp(28, 3vw, 56)px below.
- **The edge squiggle**: `<RoughAnnotation variant="box">` riding ON the panel edge, rounded corners, stroked in the panel's OWN `--acc` (round 6: a white inner line clashed): the inside half vanishes into the fill and only the wobble escaping onto the paper shows, so the edge itself reads hand-drawn. Draws once the morph settles, boils while on screen, fades as the morph releases. Sits outside the scaled visual so it is never distorted.
- **Caption ink**: the grid wrapper crossfades `--sec-ink`/`--sec-mid` from the light-theme ink to on-accent white on the same clock (color-mix over a `--wp` motion-value var), so tags and titles are white on the panel but never white on bare paper mid-transition. Card media stays `--darkpanel` (navy on blue).
- **Checkpoints (rounds 6-9)**: the morph bands are `useScrollCheckpoints` bands (dynamic-bands option, STYLE_GUIDE 7.4): the COMMITTED runway slice (MORPH_REST 0.3 -> 1) and the exit window, nothing more. The float/spin zone before the dive parks freely (the cube is alive there; round 9, the full-runway band idle-glided the whole choreography off one notch and the panel "appeared instantly"); an idle park from the dive on completes the morph at glideMsPerVh 2600 / max 5000ms so the completion plays, not cuts. The hero's four rests (incl. release) stand as before at the default glide pace. Round 7's film-to-panel mega-band stays documented in 7.4 as a hard lesson: a band must never span more scroll than its own beat.
- **The reform slab (round 7)**: in the hero's reform, the re-materializing glass alpha rides the slab's thickness (x0.25 while flat, full as it thickens into the cube), so scrolling UP melts the cube toward the film instead of parking a bright flat "white frame" beside it; scrolling down the quench reads softer but intact.

Off the grid's far edge everything runs backwards: the panel collapses to its bottom bar, the slab un-stretches into the cube, and the companion resumes the open-region journey (work waypoints 0.1 / 0.5 / 0.92 only steer the free cube at the edges of the morph). Mobile: the cube still fades out over the top of the section (v2); the panel morph is DOM-only there.

Reduced motion / no WebGL: the panel is simply there (opacity 1, scale 1, captions white, edge drawn on entry, static under reduced motion), no morph, no checkpoints.

Open with Brad after round 6: the settled state is still a lot of flat blue with placeholder cards; his call was that real case media and titles will carry it, revisit after assets land.

## Reduced motion

Settled render, normal section rhythm, hover color-only, no cube.

## Done when

- [x] 6 cards at lusion scale, every card resolving to /results/[slug]/
- [x] Release rest beat shows the headline + cube (desktop and mobile)
- [x] No invented clients, metrics, or media; placeholders visible
- [x] 375 / 768 / 1280 / 1536 no overflow; reduced motion settled
- [x] Work panel morph: cube -> blue panel -> cube, squiggle border, caption ink crossfade (round 5)
- [ ] Brad reviews: composition, panel morph feel, placeholder treatment, support copy
