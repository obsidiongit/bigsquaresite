# Proof Numbers Section Specifications

Big metrics that carry the argument. Numbers get `--acc`. Nothing here gets invented.

## Content

### Headline
"The numbers do the talking."

### Four metrics
All four need real values before this section ships. Do not use sample numbers in the build.

1. `[PLACEHOLDER: metric 1, e.g. cost per lead from the flagship case study]` with label
2. `[PLACEHOLDER: metric 2, e.g. leads generated in a set window]` with label
3. `[PLACEHOLDER: metric 3, e.g. locations served or client retention]` with label
4. `[PLACEHOLDER: metric 4]` with label

Format per metric: big number (Bluu Next Bold, `--acc`), one-line label under it (Apfel Regular, `--mid`).

## Layout Reference
- Source URL: https://www.youtechagency.com/ (93% / 9x / 3 / $10B row) and the brand sheet "Metrics" block

## Components to Use
- Four-up grid, two-up on mobile.

## Animations / Effects
- Count-up on scroll entry, 1.2s, ease-out. Static under reduced motion.

## Design Instructions
- `--darkpanel` background for contrast. Numbers `--acc`, labels `--ondarkmid`.
- Use a `metrics` array in code so values can be updated from one place.
