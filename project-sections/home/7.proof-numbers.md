# Proof Numbers Design Brief

v3, 2026-08-24. Updated in the region pivot session: the band moves from phase 2E into **2E-R** (built now with the lower-region rebuild), follows the Youtech "Anti-Agency Business Model" composition (stats grid left, media panel right), and drops the rails/registration-marks/Nº system along with the rest of the open region. SectionWipe is deferred to a polish pass. STYLE_GUIDE 6.4 (stat tile) still governs the tiles.

Role: the dark band where the argument goes quantitative. Nothing here gets invented: every value traces to a spec file or does not ship.

**Open question for Brad (carried from v2): 3 metrics or 4?** decisions.md locked 3; the Youtech 2x2 grid composition wants 4. Build renders whatever `lib/metrics.ts` holds, so this is a data decision, not a layout blocker. Default: build the grid to handle 3 or 4 gracefully.

## Content

### Headline (DRAFT)
"Built like the agency we wanted to hire."

### Support line (DRAFT, one sentence)
"No lock-in, no black box, and numbers you can audit."

### Metrics (from `lib/metrics.ts`, all placeholder until real values land)
1. `[PLACEHOLDER: metric, e.g. cost per lead from the flagship case study]` + plain-language caption
2. `[PLACEHOLDER: metric]` + caption
3. `[PLACEHOLDER: metric]` + caption
4. `[PLACEHOLDER: metric, if Brad approves 4]` + caption

### Media panel
`[PLACEHOLDER: video or photo. Until real media exists, a darkpanel ground with a centered mono placeholder note.]`

Launch gate: this section cannot go live with placeholder values. Real numbers come from Brad or Mike.

## Design direction

- Dark section (`theme="dark"`, `anchor="proof"`), full bleed, `py-section-y-lg`. Content `relative z-10` (the cube passes over dark grounds too). Width: EDGE. No GridLines, no registration marks, no Nº label.
- Two-column composition (Youtech frame 05): left column headline in `--ondark` + support in `--ondarkmid`, then the metric grid (2x2, or 3-up row if the count stays 3). Right column: media panel, `rounded-[24px]`, reuse `FramedMediaPanel` or a plain darkpanel media slot.
- Stat tiles borderless: number at `--text-metric` (Bluu 700, tabular-nums) in `--accondark` (never raw `--acc` on dark), caption below in mono uppercase `--ondarkmid`. Plain-language captions per the Youtech ANALYSIS ("93% client retention rate" style: number restated in words).
- Placeholders render honestly: mono `[PLACEHOLDER]` text, never a fake number.

## Steal from

- Youtech dark stats band: `../reference-images/youtech-agency/youtech-home-desktop-05.png`
- Flat stat tiles: `../reference-images/e2vc/pages/e2vc-impact-desktop-05.png`

## Build with

Existing: `Section` (dark), `FramedMediaPanel`, mono meta, `components/motion/CountUp.tsx`, `Reveal`.
New: `components/sections/home/ProofBand.tsx`, `MetricBlock` (shared; testimonial + case study pages reuse it), `lib/metrics.ts`.
Deferred: `SectionWipe` (polish pass, task 2K era; the band enters with a plain Reveal for now).

## Motion

- Reveal stagger on entry. Real numbers CountUp (1.4s, house ease) on first view; placeholders never count up.
- Reduced motion: settled, final values.

## Mobile (375)

Stack: headline, support, metric grid (2x2 stays 2x2; numbers at the 48px clamp floor), media panel below full width.

## Theme, number, annotations

- Theme: `dark`. No Nº label (numbered narrative ends at the hero). No annotations.

## Done when

- [ ] Numbers `--accondark`, captions mono, tabular nums verified
- [ ] `lib/metrics.ts` is the single data source; grid handles 3 or 4
- [ ] Placeholder rendering is visibly a placeholder; no CountUp on placeholders
- [ ] Media panel placeholder honest, swap is one data edit
- [ ] Reduced motion settled; screenshots at all four widths reviewed with Brad
