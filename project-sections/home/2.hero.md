# Hero Design Brief

v6.1, 2026-08-24 (Brad's live review of the card beat): the beat read as empty, only the small right column of text. It gained a large statement, lusion's big-left/small-right card screen completed: **"Proof before promises."** at `--text-display` (Bluu italic on "promises.", the page's one italic word), two lines swinging in from the TOP LEFT with per-line lag and a clearing 6px blur (mirroring the opening headline's exit direction, reversed), in 0.44 to 0.58, retreating upward through the side text's exit window (0.66 to 0.72). Headline copy is DRAFT pending Brad. The side text's rule link retargeted: featured work will land directly below the hero, so "See the Results" -> /results/ became "How We Work" -> /about/ (label DRAFT pending Brad; he only locked that it should go to the about page).

v6, 2026-08-23 (built with phases 2C/2D): the queued companion direction is live. The canvas moved out of the hero into a page-level FIXED layer (`HomeStage` + `HomeCanvas`, lusion's architecture); the hero wrapper grew by 7/6 (374vh mobile / 560vh desktop) and gained a closing beat: **the reform**, where the framed film panel un-balloons back into the glass cube, which then follows the visitor down the page as a companion object (waypoint journey through trust, problem, solution, services). All v5.1 choreography is unchanged, remapped into the first 6/7 of the wrapper (`HERO_K` in `media.ts`). Layering contract: nav z-50 > section content z-10 > canvas z-5 > section grounds and rails; the sticky hero stage carries the z-10 itself (position:sticky makes a stacking context).

v5.1 base, 2026-08-23 (v5 revised same day on Brad's live review: smoky ghost core instead of the solid block, calmer refraction, roughly half the spin, slower swoop, headline raised beside the cube, statement de-collided, and a side text block at the held card beat). v5 base: v4's Framer-Motion sheet takeover was rejected in review (execution "clunky and cheap, fragmented"; flat CSS transforms cannot deliver the morph). Same session, Brad added the missing piece: the open area above the copy gets a 3D brand object (his sketch: cube top-right, arc down into the media panel), it must be glassmorphic Spline-grade quality, the ink line is CUT, the scrub must feel slower and smoother, and the media settles as a framed panel, NOT full viewport. v5 is all of that on one WebGL canvas: **the glass square becomes the film.** Inspiration ceiling: lusion.co (their reel handoff, studied frame by frame in `../reference-images/lusion/live-scroll-capture/`, frames 03 to 09) and Spline glass renders for the cube.

Role: the page's opening statement and its first proof of craft. No selling above the fold. Conversion lives in the nav pill and the closing CTA band.

## Content

- H1 (locked A): "More locations. More revenue from each one." Circle on "each one" (kept; the one hero annotation).
- Statement (small, right-aligned column): "BigSquare is the growth partner for multi-location and franchise brands. One team runs your ads, your search, your site, and your creative."
- Instrument strip on the fold rule: "MULTI-LOCATION & FRANCHISE MARKETING" left, "SCROLL ↓" center, "©2026" right (13px mono).
- Film meta on the settled panel: "BIGSQUARE / BRAND FILM" left, "Nº001 / ©2026" right (year hidden below sm). "+" registration marks outside the panel corners, md and up.
- Card-beat headline (v6.1, all sizes, top left above the held card): "Proof before promises." at `--text-display`, italic "promises.". DRAFT pending Brad. Not an H1 (the page's H1 is the opening statement); rendered as a styled `<p>`.
- Card-beat side text (md+, right column, slides in while the film card holds): mono eyebrow "THE BRAND FILM", lead paragraph, and a "How We Work" rule link to /about/ (v6.1 retarget; label DRAFT pending Brad). CURRENT PARAGRAPH IS DRAFT pending Brad's copy: "One team runs your ads, your search, your site, and your creative. This is the work in motion." (first sentence reuses the approved statement copy).
- NO CTA buttons in the hero (the card-beat rule link is a showcase link, not a conversion CTA). NO ink line (cut in v5 review). No hairline rails.

## The set piece

A 320vh (mobile) / 480vh (desktop) wrapper drives a sticky 100svh stage. DOM scrub values are pure functions of native scroll progress; the canvas actors follow a TIME-DAMPED follower of that progress (rate 4.5/s), which throttles fast flicks into one legible motion without hijacking scroll. The canvas sits UNDER the text layer: the cube passes behind the exiting headline.

1. **p=0, the statement.** H1 at `--text-display` raised into the space beside the cube (flex spacers 1.15 above / 1 below, not bottom-anchored); words rise on mount (0.65s, house, 30ms stagger, done under 1s); circle draws after the H1 lands, then boils. Statement paragraph in its own clear row at the bottom right (never under the headline). Instrument strip on the fold. In the open area top-right floats THE SQUARE: a glass cube (transmission, ior 1.25, thickness 0.6, roughness 0.09, RoomEnvironment) with a SMOKE CORE inside: a single amorphous wisp, vertex-noise morphed and fbm-density shaded in the brand blues, drifting slowly, ghostly. At rest the cube drifts (ambient sine bob) and answers the pointer with a damped tilt. Desktop anchor (0.30, 0.24) of stage from center; mobile (0.24, 0.30), clear of the 5-line headline.
2. **p 0.08 to 0.46, the swoop.** The cube dips down across the stage behind the blurring, exiting headline (headline lines drift up with per-line lag and 0 to 4px blur through p 0.04 to 0.42), shedding a gentle fraction of a turn (max ~0.5 rad added on y), and settles face-on at the card anchor, lower left (mobile: lower center).
3. **p 0.38 to 0.48, the flatten.** The cube compresses into a thin glass pane exactly the size of the media card (16:9, 0.34 of stage width desktop, 0.62 mobile). The smoke shrinks away; the frost sheds (envMapIntensity and roughness ramp down) so it reads as a clearing pane, not a milky slab.
4. **p 0.44 to 0.72, the held card + develop.** The display headline swings in from the top left (line 1 in 0.44 to 0.55, line 2 lagging 0.47 to 0.58, blur 6 to 0; both retreat upward and fade 0.66 to 0.72), and the side text block slides in from the right (in 0.46 to 0.54, out 0.66 to 0.72), while the film surfaces ON TOP of the glass pane (renderOrder above it, slight z lead; gate 0.50 to 0.58), fading up inside the same footprint like frost clearing; the hidden glass is then retired. The film arrives as brand-ink duotone (`#0A2A73` to `#6E9BFF` by luminance) and develops to true color through p 0.50 to 0.70 (lusion's tint-to-color move). This is the page's lusion "card + paragraph" screen.
5. **p 0.62 to 0.92, the balloon.** The card grows out to the framed panel with the cloth morph: every vertex carries its own growth progress with the trailing corner lagging (uLag 0.34, normalized so all vertices land exactly at 1), plus a z arc toward the camera mid-growth. The sheet shears and curls like dragged cloth and resolves perfectly flat. Media counter-scales 1.16 to 1; corner radius holds at 24px (`--radius-media`) from card to panel.
6. **p 0.92 to 1, the hold.** The panel settles at 4vw margins on all sides (media as an object, lusion's settle: NOT full bleed), radius `--radius-media` 24. Mono film meta fades up inside the panel; registration marks outside its corners. (All p values above are in the v5.1 clock: multiply by HERO_K = 6/7 for the raw wrapper fraction.)
7. **raw p 6/7 to 1, the reform (v6).** Still pinned: the meta fades, the panel un-balloons back to the card footprint with the reverse cloth bend while the film re-inks to the duotone; glass re-materializes over it as the film quenches; the slab thickens back into the cube and the smoke core re-ignites. In the same beat the material crossfades from transmission glass to true alpha glass (raw-scroll gated) and the in-canvas paper backdrop retires: from here the REAL page composites through the cube. Then the section releases, the info bar grounds the region, and the cube stays behind as the companion.

**The companion (v6, lives in HomeCanvas):** a waypoint journey keyed to `data-cube-anchor` sections, positions eased by a damped follower (4/s), idle turn + pointer tilt still alive. Desktop: glides right through the trust strip, holds in the problem section's right whitespace and TICKS A QUARTER TURN as each numbered row passes (steps4 easing over the rows), dives behind the solution bento toward the bottom-right, crosses the viewport foot to re-emerge left, climbs the services rail, then spins up, shrinks, and dissolves at the section's end. Mobile: one graceful drift-up-and-fade over the trust strip. Cube always BEHIND the ink (content z-10 over canvas z-5), over grounds and rails. Extend the WAYPOINTS array when later sections (proof band, portal) want the object back; a fast flick past the hero never replays the film (companion mode takes over on raw scroll, follower converges at 12/s).

## Engineering notes (learned in this build; keep)

- All DOM scrub bindings use callback-form `useTransform` with explicit clamps (framer-motion 13.1.1 range-array drift).
- r3f: NEVER pass a `uniforms` object to `<shaderMaterial>` as a JSX prop; the material ends up bound to a clone and useFrame mutations never reach the GPU. Build ShaderMaterials imperatively and attach with `<primitive object={mat} attach="material">`.
- three transmission: transparent materials are EXCLUDED from the transmission buffer. Anything that must be visible through the glass (the smoke) stays opaque (shrink it away, do not fade); anything that must cover the glass fades in above it (the film pane). Transmission + opacity fades do not composite reliably.
- The smoke core is "transparent" via stochastic coverage: an opaque ShaderMaterial whose fragment discards by fbm density against a spatial hash (a hand-rolled alphaHash). That keeps it in the opaque queue (visible through the glass) while the frosted glass blurs the grain into smoke. Its max noise-displaced radius must stay under the half-cube (scale 0.30 with the current displacement) or it pokes through a face and reads as a decal.
- Raw ShaderMaterials run in raw sRGB end to end: `NoColorSpace` on textures AND on token colors (`new THREE.Color().setHex(hex, THREE.NoColorSpace)`), `flat` on the Canvas. Otherwise the brand blues wash out.
- Budgets (STYLE_GUIDE 7.9): canvas lazy via next/dynamic ssr:false, frameloop "never" when the hero leaves the viewport (IntersectionObserver, video paused too), DPR watchdog (>34ms frames sustained steps DPR down 0.25 toward 1), everything disposed on unmount. LCP is the SSR headline. Measured 2026-08-23: the lazy 3D chunk (three 0.185 + r3f 9 + HeroCanvas) is ~232KB gz, over the 200KB written budget; three core alone accounts for most of it. Flagged to Brad; STYLE_GUIDE changelog carries the note. No WebGL → DOM fallback (simple rise into the framed panel, no cube).

Media contract (decisions.md): poster paints first (`public/media/hero-poster.jpg` as a texture; DOM `<Image>` in static/fallback paths); the loop mounts after hydration and cross-fades in once genuinely playing. Real 4K film = one-line change to `HERO_VIDEO` in `Hero.tsx`. Current placeholder loop: `public/media/hero-loop.mp4` (12s seamless ffmpeg breathe, 353KB).

## Reduced motion

Static composition: statement viewport (min-h 82svh, circle pre-drawn, no boil), then the film as a settled framed panel (4vw margins, radius 24, poster only, no autoplay) with meta, then the info bar. No pin, no scrub, NO canvas at all (the 3D chunk never loads).

## Mobile (375)

Same set piece at 320vh. Headline wraps ~5 lines at the 52px floor; circle holds "each one." Cube floats higher (0.24, 0.30) and swoops to a centered card (0.62 width). Strip drops the year and shortens the left label; film meta keeps only the left label; registration marks hidden below md. Panel margins stay 4vw.

## Done when

- [x] Statement fold matches at 1280/1536; type at true display scale
- [x] Glass cube reads premium (transmission + core) and passes behind the text on scroll
- [x] Seamless chain: cube -> glass pane -> film develop -> cloth balloon -> framed panel; no fade-out/fade-in seam
- [x] Scrub throttled by the damped follower; whole animation legible on a fast scroll
- [x] 375/768/1280/1536 pass, no horizontal scroll; reduced motion renders the static composition with no canvas
- [x] Film swap is one line; loop autoplays muted only outside reduced motion; poster-first
- [ ] Screenshots reviewed with Brad; cube look signed off against his Spline references (attachments did not reach the agent; re-share if the look misses)
- [ ] Bundle overage decision: accept ~232KB gz for the lazy 3D chunk or fund a slimming pass
