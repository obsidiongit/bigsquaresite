# Testimonial Card Component (shared)

Props: quote, name, company, role (optional), metric (optional), metricLabel (optional), caseStudySlug (optional), sourceLogo (optional: Google, Clutch).

Layout: quote in Bluu Next Italic 28px (22px mobile), name Apfel Fett, company `--mid`. If a metric exists, show it to the right as a big number in `--acc` with its label.

Rules: real names and companies only.
